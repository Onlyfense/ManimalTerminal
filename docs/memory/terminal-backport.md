---
name: terminal-backport
description: "Terminal map backport kickoff — staging done, export done, host slot verified; follows the Icebreaker playbook"
metadata: 
  node_type: memory
  type: project
  originSessionId: d0a95716-c650-4f02-a997-b6b21f9e3c7e
  modified: 2026-07-26T20:48:32.034Z
---

Terminal (retail 1.0) backport started 2026-07-26, following [[map-backport-playbook]] (docs/MAP-BACKPORT-PLAYBOOK.md in ManimalIcebreaker).

- Staging: `C:\Users\peard\Desktop\TerminalLevels` — levels 600–637 + 650, all sharedassets/resources containers, 34.67 GB, size-verified against retail. Terminal levels carry NO .resS siblings (art all lives in shared containers).
- The map ships TWICE in build settings: block 1 = 600–637 (staged), block 2 = 651–689 — almost certainly the two port-event states (PortCutscene Intro/Attack/Ending scene names). Block 2 not staged.
- AssetRipper export: `TerminalLevels\AssetRipper_export_20260726_173044` — 39 scenes exported.
- Host slot: `Terminal` is a FIRST-CLASS property on SPT's Locations record, dormant stub (Locked, empty Scene, no exits/spawns), own Id "Terminal" + mongo `5704e5a4d2720bb45b8b4567`. No slot hijack or locale rebrand needed (unlike Icebreaker/Suburbs). Shoreline's vanilla transit SHO_TRANSIT_25 already targets it.
- PhysicsSetup extracted (scratchpad terminal_levelphysics.json): bounds centre (0,0,0) size 1567x327x1714, subdivisions 16. Bake it in the editor pass from day one — this is the component whose absence caused Icebreaker's transit-arrival PhysX 64K crash.
- SDK import convention: `Assets/Terminal_Import/Content/Locations/...`; bring scenes + art, NOT the export's Assets/Scripts stub folder (GUID-collides with the SDK's existing stubs).

- SDK import DONE: dependency-closure copy (scratchpad terminal_import.py) — 18,724 assets, 8.95 GB into `Assets/Terminal_Import` with `_import_manifest.txt`; wholesale copy was 56 GB, Icebreaker_Import precedent is 4 GB curated.
- AssetRipper script GUIDs are PER-RUN, not deterministic: terminal scenes all showed Missing Script. Fixed by guid rewrite (scratchpad terminal_script_rebind.py + terminal_package_rebind.py): 91 game classes -> SDK stubs, 8 package classes (Timeline/uGUI/TMP) -> Library/PackageCache guids, 164 files rewritten. Trap recorded in the playbook.
- ~243 classes still stubless (stub backlog, top: StaticDeferredDecal, MultisceneSharedOccluder, SmartGrip/LimbIK, StencilShadow, Culling*Light*, FlareLight/AreaLight, Handler* cutscene set). Next step: auto-generate stubs from SPT Managed typetrees (TypeTreeGenerator), the LevelPhysicsSettings.cs pattern industrialized. Unity.AI.Navigation package is NOT installed in the SDK (NavMeshModifier refs dangling) — install via Package Manager when navmesh authoring starts. Meta.XR.Audio refs: skip, acoustics get rebuilt anyway.

- Shader/material state (all tooling + audit in TerminalLevels\analysis): decals routed to Custom/Icebreaker Decal (175 mats), VP Solid -> Custom/Icebreaker Vert Paint Blend (11), puddles -> NEW Custom/Icebreaker Puddle shader (11 mats, fingerprinted by _RippleScale/_EditorWaterLevel; deterministic guid md5('manimal:IcebreakerPuddle.shader')). 4 SoftCutout Decal mats + fuel-slick look + UnderwaterSurface = open shader backlog.
- Shader audit (terminal_shader_audit.txt): 45/52 ripped shaders exist in 4.0.13 -> covered by porting the icebreaker raid-start rebind (Phase 7). CAVEAT: name-existence != rebindable — deferred decal/VP shaders exist in 4.0.13 but need the game's decal-system components to render, which is why the editor-side swaps were done anyway. 7 genuinely missing: ANGRYMESH PBR Rocks (terrain), Cloth x2, 3 explosion/particle FX, Hidden/Water/SurfaceBack — stand-in decisions when seen broken.

- Cutscenes: Terminal has TWO (intro at map start + a timed second one). Terminal_Cutscene_Content (level630) is a 1-GameObject holder — empty in retail too, not a rip loss. Real content: CutscenesSources scenes, exported to Terminal_Import/CutscenesSources/Scenes — PortCutscene_00_Intro (level631, 3680 GOs, 57 MBs), 01_Attack, 02_Ending_00. Intro references resources.assets (Timeline data lives there, AssetRipper husks it) — resources.assets/.resS/.resource re-copied into TerminalLevels for the extraction. Rebuild = the icebreaker chain: extract_cutscene_* + gen_cutscene_build_data (m_StopTime clip fingerprints) -> Author-17-style editor rebuild -> client driver ported with a MAP-START trigger (not a story trigger). User wants intro only for now.

- Intro cutscene chain DONE up to the editor rebuild (all scripts in TerminalLevels\analysis): pass1 (level631 wrappers — director has NULL playableAsset, the BSG CutscenePlayableDirector assigns it at runtime), pass2 graph with ROOT DISCOVERY via binding-key overlap (root = resources.assets pathID 45846 'Terminal_Area_PortCurscene_00_Intro_TimelineDirector' — BSG's typo; 100% stock Unity Timeline: 81 tracks/74 playables, no BSG track classes, WIP assembly not needed), 32/32 anims + 6/6 audio matched (m_StopTime fingerprints) and copied to Terminal_Import/CutsceneAnims (TRM_CS_ prefix), build data generated + bindings enriched 41/41 (40 Animators + 1 MonoBehaviour under CutsceneRoot/...). SDK Author 21 = TerminalCutsceneTimeline.cs (menu Terminal/Author 21): per-clip audio, per-track emitters via markerPids (TWO signal tracks — Author 17's all-emitters-on-every-track would double them), MarkerTrack skipped, case-insensitive director find. CONFIRMED WORKING 2026-07-26: user ran Author 21 + 1LD, cutscene previews in the Timeline window. Remaining: playback driver (Phase 7 client, MAP-START trigger, port of IcebreakerTimelineCutscene) + the second cutscene (01_Attack, timed — same chain, different scene seed) later.

- Lighting: 39/39 scenes have LightingData.asset; Author 1LD generalized (resolves from scene path, serves both imports) — restores probes/ambient/reflection. Per the user, lighting is ALL DYNAMIC lights, same as icebreaker — the lightmap-bake path (Lighting 0/2 tools) went nowhere on icebreaker and is NOT wanted for terminal. Real lighting work = client runtime (ambient fill, lamp driving, culling-light restore).

**Why:** next session should resume at the stub generator without re-deriving any of the above.

**How to apply:** read the playbook first; extractors in ManimalIcebreaker/analysis are parameterized by LEVELS_DIR + LEVELS range (600–638 for Terminal).
- Attack cutscene "empty shot" postmortem (2026-08-09, frames 1374-1500 / 22.9-25.0s): TWO separate causes. (1) Scav04's shot-hold clip (clipPid 3300 'Recorded (5)', 0.52s) never matched — constant hold clips have m_StopTime EXACTLY 0.0 on both retail and export sides, and the matcher's `(stop or 999)` falsy-zero fallback turned the perfect match into the worst score. When his run anim (Recorded (25)) ended at 22.9s the empty clip snapped PosAdjust back to origin, teleporting him 17m out of frame for exactly the shot window. Fixed in attack_match_anims.py + match_terminal_cutscene_anims.py (explicit None checks); 146/146 now match; same fix applies to any future map. Zero-stoptime "unmatched Recorded" clips are NEVER benign — they are the hold poses. (2) The "vehicle" he shoots over is a Uaz_military prop that lives in Terminal_LIGHT.unity (BSG parents props with light rigs into the light scene — checkpoint UAZ is under SBG_Terminal_Light/OO/LIGHT_with_no_Culling/Terminal_Area_01_CutScene_Outdoor/LIGHT/OFF/). It is intact in the SDK and in the bundle; it only looked missing because Terminal_Light.unity was not loaded during Timeline preview. Lesson: when previewing cutscenes, additively load Terminal_Light. Known remaining cosmetic: Utes 'Take 001' deploy anim has AssetRipper hash-only curve paths (path_0x..) so it binds nothing — mount stays static during the 3.4-10.1s load beat; fixable later by CRC32-resolving paths against the Utes subtree.
- SOUND rig orchestrator REWORKED to the icebreaker AmbientAudio model (2026-08-09, builds clean): the ambient player/spline stack is ALIVE in 4.0 Assembly-CSharp (verified per-class against the DLL) — OneShot/AmbientSoundPlayer, AmbientSoundPlayerGroup, AmbientPlayerGroupSplineEmitter, SplineEmitterPathMover, SoundPlayerSplineTrigger, AmbientSplineEmitterController, BezierSpline, EFT.SoundBank. terminal_sound_components_spec.py emits plugin-data/sound_rig_components.json (79 components, icebreaker-style $AudioClip/$asset:SoundBank/$ref tags) + sound_rig_banks.json (19 banks/40 clips). TerminalSoundRig.cs: inactive-root two-pass restore (players first — [RequireComponent] on abstract BaseAmbientSoundPlayer), reflection SoundBank rebuild (Environments[0][0], HasEnvironment off), ClampForever on all restored curves (looping rolloff = map-wide audio, icebreaker lesson), spread is INVERTED 0-1 (Lerp(180,0,v)). Phase gating stays ours (cutscene event filters dead): between/after group SetActive + BSG Play() via reflection with direct-clip fallback. Firefight sim + sirens unchanged (director classes dead). NEW post-attack door beat: door_Terminal_Area_01_CutScene_00000 unlocks (Locked->Shut + KeyId cleared) + AdditionalSounds plays — DoorOpenSound = entity_cardlock_doorunlock (exported from sharedassets54:1363; the rig's ref misparsed to a GameObject pid), foley +1.2s; VSRF voice line is dead-localized-player-fed, stays silent. NEW camera shake: TerminalAttackCutscene scans its own rebuilt timeline for CameraShake_01 SignalEmitters (7) and applies decaying perlin wobble in DriveCamera (ExplosionShaker chain dead). csproj now references Unity.Timeline. Still not ported: EnvironmentSwitcher spatial rooms (blocked on SpatialAudioSystem being skipped), mixer fade handlers (subsumed by group deactivation). User editor steps: Author 26 (now 41 clips incl the door unlock), save Design_Stuff, rebuild bundle.
- Door foley RESOLVED per-family (2026-08-10): the doors json's loose parse had misaligned sound refs AND the WIP tree has its own Door drift (14/60 fail, rest read empty) — anchor-carve found the real refs but the RETAIL INSTALL UPDATED SINCE THE RIP (current ggm resolves level script pptrs to NOTHING — always pair level files with TerminalLevels' own globalgamemanagers.assets; sharedassets5's pids shifted non-uniformly, no clean delta). Exact pids unrecoverable -> semantic family mapping in extract_terminal_door_sounds.py: 60 doors -> plastic(13)/wood(6)/grate(15)/metal1(26) by prefab name, arrays emitted PRE-BUCKETED with sibling-ordinal (name~k) paths (60/60 match terminal_doors.json). Retail door clip families found by name sweep across all 291 level externals (terminal_door_clip_inventory.json): door_plastic_*, door_wood_modern/old_*, door_grate_*+barbwire2, metal_door_1/2_* incl ruchka handle clips; 29 clips exported to Terminal_Import/DoorSounds. Author 1R patched: uses bucketed arrays VERBATIM (old substring re-bucketing dropped barbwire2/ruchka), per-door HitClip/BreachSound, DoorSounds dir first in WavDirs. BONUS: vsrf_door_open_voice_ru/_eng found in sharedassets628 (the "unrecoverable" localized door voice) — both exported to SoundRig, door beat now plays unlock (0s) + foley (1.2s) + eng voice (2.2s). User editor steps: re-run Author 26 (SoundRig gained 3 clips: door unlock + 2 voices), re-run Author 1R, save scenes, rebuild bundle.
- LIVE 1.0 DUMPS integrated (2026-08-10, from G:/downloads/terminal): 8 client/match/local/start responses (data.locationLoot = the FULL live Location object) + 23 bot-generate dumps (1086 bots). terminal_live_loot.py -> db/staticContainers.json (224/231 containers; 3 1.0-only container tpls remapped to 4.0 pool-mates by content overlap, scene binding is by Id so tpl only picks the pool), db/staticLoot.json (22 pools), db/looseLoot.json (47 spawnpoints + 383 FORCED - dead-body/preset/vog battlefield dressing is IsAlwaysSpawn in live; mean 414 loose/raid); 78 unique 1.0-only tpls trimmed (~330 instances, incl quest notes). terminal_live_base.py -> base.json 3KB->135KB: 109 SpawnPointParams (104 Savage + 5 Pmc, east/west infils), 68 BossLocationSpawn (assault 40, blackDivision 15, vsRF 8, civilian 3, vsRFSniper 1, bossTagilla 1 - BD/VSRF roles resolve via the WTT BlackDiv/MoreBots deps), 40 waves (all native assault), 5 exits (live ships Chance 0 = transit-gated; bumped to 100: Unity_free_exit/Sniper_exit/Nakatani_stairs free, Sandbox_VExit TransferItem, Scav_coop_exit ScavCooperation), BotLocationModifier + full bot count/timing (BotMax 30, BotStart 420, BotStop 2800). Preserved overrides: Id/_Id/Name/Locked/Enabled/Scene/EscapeTimeLimit/DisabledForScav/GenerateLocalLootCache. Server builds; all 4 db files deploy to SPT/user/mods/ManimalTerminal/db. FUTURE: bot dumps carry full live BD/VSRF loadouts (Inventory trees) - loadout enrichment for BlackDiv bot types not yet mined; navmesh/AI bake still pending for bots to actually path.
- Boss role remap finalized (2026-08-10, user call): civilian rows DROPPED (3); blackDivision -> blackDivAssault (TacticalToaster/BlackDiv enum 848421); vsRF -> ruafRifleman (848400) and vsRFSniper -> ruafMarksman (848404) from TacticalToaster/RUAFComeHome — NEW DEPENDENCY added to TerminalMod.cs: com.ruafcomehome.tacticaltoaster >=1.1.0 (guid from repo Server/Mod.cs, v1.1.2, needs morebotsapi+commonlib+contentbackport itself). Other registered roles available if wanted later: blackDivLead/Breacher/Support, bossWedge, blackDivIb; ruafRiflemanSenior/Autorifleman/Grenadier/Machinegunner, remnantRifleman. Remap lives in terminal_live_base.py (covers BossName, BossEscortType, nested Supports[]). base.json now: 65 boss spawns (assault 40, blackDivAssault 15, ruafRifleman 8, ruafMarksman 1, bossTagilla 1). Server builds clean.
- Hostility matrix authored (2026-08-10, user calls): TERMINAL-ONLY by construction — lives in our base.json BotLocationModifier.AdditionalHostilitySettings (7 entries from live, role-remapped), other maps' RUAF untouched. ruafRifleman/ruafMarksman: Bear/Usec/SavagePlayerBehaviour explicitly 'Neutral' (live shipped vsRF with NO player keys = neutral by default; made explicit — neutral bots retaliate when shot, the requested behaviour) + AlwaysEnemies vs blackDivAssault/assault/assaultGroup. blackDivAssault: 100% ChancedEnemies vs both PMC factions (live values) + AlwaysEnemies vs RUAF/scavs. Scavs: enemies with PMCs/BD/RUAF. civilian stripped from all lists. RUAFComeHome's own plugin does NOT hard-set hostility (BotsControllerInitPatch only inits checkpoint managers) so location settings govern. Verified in generated base.json.
- Loot tpl filter fixed + container Id rebake (2026-08-10): RULE (user call) — before dropping a tpl, check the installed mods' CustomItems configs (WTT-ContentBackport carries most 1.0 items). terminal_live_loot.py allow-set = vanilla + 1564 modded tpls (Armory 822, ContentBackport 593, ...); dropped fell 78->8 tpls (only true-missing quest notes remain), forced spawns 383->390. Checkpoint keycard 6866ad3853330f9b83064cf9 (the Administration keycard-door key, ContentBackport-provided) FORCED into the design-stuff suitcase via staticForced {containerId: container_Terminal_Design_Stuff_00186, itemTpl} (bigmap-verified shape). Armory key 68a4b745b63e97cc5a056a07 restored: fixed forced spawn (206.7,-58.6,-237.1), 3-position group spawn arc_key_alt_1 (incl the Corpse_RUAF_06 point at 123.7,-60.4,-403.2), chanced Substation container. CRITICAL SCENE GAP FOUND: the rip left EVERY LootableContainer Id EMPTY (no server binding = no container loot at all). extract_terminal_containers.py: typetree reads Id empty (drift) but raw bytes carry Id + Template tpl as strings in field order -> string-carve; 255/256 recovered (the 2-char 'n1' id lost to carve threshold). NEW Author 27 (TerminalContainerIds.cs): position-matched Id+Template rebake. User steps: Author 27 on open Terminal scenes (Design_Stuff has 255), save, rebuild bundle.
- Loot no-spawn postmortem (2026-08-10): TWO stacked causes. (1) looseLoot.json: my synthetic merged-variant item ids were decimal strings — SPT's MongoId converter hard-rejects non-24-hex and jsonUtil SWALLOWS the exception into a silent null, so the mod's 'missing' warning was misleading (the else branch catches both not-exists and null-parse). Fixed: md5(lid:ck:i:j)[:24] hex ids; ALL THREE db files now verified against SPT's REAL serializer via a scratchpad net9 console app that constructs SPT's JsonUtil with its own SptJsonConverterRegistrator (LootTest — keep this trick: it surfaces the exact JSON path SPT would silently swallow). (2) the raid was played against the stale factory fallback (ids dont match the map = zero loot). Container loot additionally needs Author 27 scene ids IN the built bundle. Debug rule: [Terminal] boot lines are the loot health check — want 'container loot pools loaded (22)', 'authored loose loot loaded', 'container set loaded from bundle scan'. NEW Author 28 (TerminalFadeCanvas.cs): authors the UI_FadeToBlack rigs scene-side in both cutscene scenes (overlay@9999, 1920x1080 scaler, black rgb + full-stretch, raycast off) — the rip broke render mode/scaler/color; runtime driver fixes stay as fallback. User steps: Author 28 on both cutscene scenes, save, rebuild bundle; restart server and check the three loot lines.
- Fade anim curves fixed (2026-08-10): the three fade clips (intro TRM_CS_3329_Recorded_1 62s master fade; attack TRM_CSA_3319/3450 in/out) were dead TWO ways — AssetRipper hash-placeholder attribute (script_0x1222D899 -> m_Color.a) AND a synthetic UI-assembly script guid on the curves (3cf5a444... -> real UnityEngine.UI.Image fe87c0e1cc204ed48ad3b37840f39efc; unity binds MonoBehaviour float curves by path+classID+SCRIPT GUID+attribute, so either mismatch kills the bind). Patched in place — no Author re-run needed (timeline references the same .anim asset paths); unity reimports on focus. These were the only script_0x casualties in the whole CutsceneAnims set. Diagnostic chain for 'scrubbing does nothing': binding scenePath ok -> Animator present -> anim attribute/guid vs scene component. Fade rig at rest is OPAQUE now that the canvas works — editor QoL: toggle UI_FadeToBlack off while composing (driver SetActive(true)s it in game regardless).
- Fade-rig rest-alpha trap (2026-08-10): Author 28's canvas fix made BOTH scenes' fade overlays functional, and the authored rest pose is alpha=1 — so with both cutscene scenes open in the editor, the scene you are NOT scrubbing is a working opaque ScreenSpaceOverlay@9999 black sheet over the entire Game view (looked like 'camera renders nothing'; disabling the CURRENT scene's fade changes nothing because it is the OTHER scene's copy). Author 28 now sets rest alpha 0 — safe in-game because the intro master fade drives alpha across its whole 62s, the attack fade-in clip starts at t=0 with value 1, and the drivers' own OnGUI fade covers scene entry/exit. Debug rule: a fully-black Game view with working overlay canvases = check EVERY loaded scene's overlay canvases first, not the camera.
- Night clock + lootability + door-open (2026-08-10): NEW TerminalRaidClock — RaidStartHour config (default 22.0, -1 off) anchors the raid clock at raid start then lets it tick; resets BOTH GameWorld.GameDateTime and GClass4.CurrentTime's GameDateTime (can be distinct instances; sky follows via TickSkyTime). Containers not lootable = (a) that raid ran the stale factory-fallback loot, (b) terminal scenes have NO LocationScene components — the game enumerates scene content through LocationScene arrays, not FindObjectsOfType (icebreaker Author 5/11 lesson). NEW Author 29 (TerminalLocationScenes.cs): creates LocationScene per scene, wires LootableContainers + WorldInteractiveObjects, empty-sweeps other arrays (lamps/audio deliberately NOT wired — our drivers own those). Cutscene door didnt open: retail authors Snap = Locked|Shut ONLY (the EVENT opens it, not players) — door beat widens Snap to include Open + unlocks (Locked->Shut, KeyId cleared) — NO auto-open (user call: the player opens it themselves; the Snap mask missing Open was why interaction refused). Compiler-verified APIs: GameDateTime.Reset(DateTime), Door.Snap, InteractionResult. NAVMESH RECOVERED (user pushback was right): NO map keeps NavMeshData in its LEVEL files — icebreaker's levels are equally empty and every NavMeshSettings.m_NavMeshData pointer ships NULL; the mesh lives in a SHAREDASSETS. Terminal's is sharedassets635.assets (Terminal_AI sibling) pid 2 'NavMesh-Global' 4.7MB — found by scanning all 291 level-external files. Exported via headless AssetRipper -> Terminal_Import/NavMeshData/NavMesh-Global.asset (9.7MB yaml; + NavMesh-NavMesh.asset from sharedassets1, probably another map's). NEW Author 30 (TerminalNavMeshRestore.cs, icebreaker Author 23 port): assign to ACTIVE scene (Terminal_Scripts) NavMeshSettings, SAVE, rebuild. Meshless = floating stacked-bot piles with zero editor symptom. AI-bake port (task #8) still pending for bot POINTS. User steps: Author 29 with all scenes open, save, rebuild bundle.
- NavMesh asset finalized (2026-08-10): the headless AssetRipper run exported BINARY .assets (icebreaker's July run produced YAML — settings drift); binary imports questionably and, worse, the retail mesh ships agentTypeID -334000983 (BSG custom) which has no agent entry in the SDK project = NO editor preview and default NavMeshAgent queries (SPT bots, typeID 0) MISS the surface. convert_terminal_navmesh.py: UnityPy-parses the binary and re-emits icebreaker-shaped YAML with agentTypeID -> 0 in both m_AgentTypeID and m_NavMeshBuildSettings (icebreaker's working mesh is typeID 0 — proven treatment). NavMesh-Global: 972 tiles, 0 offmesh links, 9.5MB yaml. User: Unity refresh, re-run Author 30 Assign NavMesh-Global (previous assign may have bound the binary import), save Terminal_Scripts, rebuild; preview needs the AI Navigation overlay's Show NavMesh checked.
- Navmesh CONFIRMED in-editor (2026-08-10): user assigned NavMesh-Global via Author 30, cyan preview draws. Retail pathing surface fully recovered.
- AI BAKE PORTED (2026-08-10, builds clean): extract_terminal_aibake.py + extract_terminal_aiplaces.py (param-patched icebreaker scripts, level635) — EVERY class byte-exact (same 1.0 drift surgeries hold): 2.5MB AICoversData, 1.6MB voxel grid, 705 mines, 30 core points, 49 NavMeshDoorLinks, AIPatrolsData. NEW enrich_terminal_patrols.py recreates the lost icebreaker patrol post-pass: 1568 PatrolPoints + 35 PatrolWays (4.0-tree byte-exact) + 35 BotZone memberships by raw ref-scan (BotZone drifted). Client: TerminalAIBake.cs (icebreaker port; TryFill in AICoversData.RestoreData prefix + finalizer swallow, patrol navmesh snap with the triangulation + hit-rate guards, MidOpen/MidClose door-link recompute, carver rebuild) + TerminalAIPlaces.cs (BotsController.Init postfix; 17 trigger boxes + 16 TierEventLogic — human-gated EventRaise reimpl; NO group-size class on terminal; no crew bridge needed, BSG BossSpawnScenario subscribes natively). KEY: 55/65 live boss waves are botEvent-gated with TriggerIds matching the place EventNames (TB1-TB8, T0-T5, 'Waaagh') — without places the spawn choreography never fires. Sidecars: plugin-data/aibake/terminal_aibake.json (31MB) + plugin-data/aiplaces/. Scrub list AI entries REMOVED (TryFill + RaidFixes null-heal own the holders now). Config RetailAIBake (default on). csproj += UnityEngine.PhysicsModule + AIModule. Remaining for bots: fika-teammate gating in TierEventLogic (IsYourPlayer solo-only), first live bot raid validation.
- AI bake first-raid autopsy (2026-08-10): fill itself LANDED (11956 covers, 7732 ways, 28900 voxels, 1568 patrols snapped 278/0.15m worst) but RestoreData threw KeyNotFound '0' -> half-restored holders NRE'd through BotsController.Init into the matching-screen popup. ROOT CAUSE: retail's own bake carries ONE orphan GroupPointWay (Dist 0, IdTarget 0; point ids start at 3) — 1.0's RestoreData tolerates it, 4.0's dictionary indexer throws. Referenced by nothing. FIXES: TryFill drops ways targeting nonexistent point ids; FillFields skips retailExtra_* bookkeeping keys (705x warn spam); finalizer now clears Points/Ways/Pathes to coherent-empty on swallowed failure (half-state was the popup). CORRECTION (second autopsy): the 43k dangling NeighbourhoodsWaysIds are NOT tolerated — icebreaker's data has ZERO missing refs; 4.0's RestoreData return-false's on the FIRST miss (silent unity LogError only), InitForGamePoints never runs, Childs stays null, CachePoints NREs. Decompiled source (D:/SPT400_assembly) was the breakthrough — retail terminal serialized only 7732 of 51246 referenced ways (1.0 restore is tolerant, 4.0 isn't). Fix: TryFill strips dangling neighborhood refs JSON-side before the fill (id-space reconcile, three passes). Also audited the rest of the restore chain: GetCorePoint = FirstOrDefault (tolerant), danger places empty, voxel point refs 0 missing. AICorePoint id lives in _id (not Id) — fine.
- Icebreaker bot-hardening set ported (2026-08-10, TerminalBotFixes.cs, builds clean): Patch_CoversCache (CachePoints finalizer — degrades to lazy cache), Patch_BotDoorsRefresh (BotDoorsController.RefreshData swallow), Patch_PlaySoundAirbag (bot-hearing dangling-ref NREs, rate-limited), Patch_DoorTriggerEmit (WorldInteractiveObject.method_3 — dead quest-trigger singleton NREs after EVERY door swing on backported maps; vanilla maps keep theirs), Patch_BotActivationStepwise (THE icebreaker scar: mirrored 49-step method_10 with per-step guards + named failure logs — one broken subsystem no longer silently statues the bot; skips original, BotState=Active mid-list as BSG authored), TerminalStuckBots watchdog REMOVED (user call — not wanted on terminal; icebreaker-specific selection-behavior remedy). DissonanceVoip ref dropped with it. NOT ported: IceCrewJobs bigbrain idle layer (fed by icebreaker's crew system; only needed under SAIN — if SAIN idle-statue shows in testing, adapt it with a zone-patrol job source) and synth cover/zone generation (terminal has the retail bake).
- Bot-raid triage round 2 (2026-08-10): bots WERE generated (server log: 180 incl blackDivAssault/ruafRifleman/bossTagilla — remap + freecam-tripped botEvent triggers all working) but never PLACED: BotsController takes its BotZone[] from the LocationScene registries and Author 29 v1's 'safe minimal set' left BotZones/ExfiltrationPoints/etc EMPTY. Author 29 now wires FULL icebreaker-Author-5 parity (zones, spawn markers, exfils, door links, places, lamps, audio + containers + interactives). Sky stayed daytime: single-shot clock postfix only caught GameWorld's clock ('1 clock(s)'); GClass4's (the one TickSkyTime reads) wasn't up yet — RaidClock is now a 20s retry host anchoring BOTH and reporting which landed. Doors 'sound but no motion' = classic STATIC BATCHING symptom — TerminalUnstatic (Author 1U) must run AFTER the latest Author 1R re-run and be in the built bundle. Containers no-prompt = LocationScene registries missing from the built bundle (game binds via LocationScene.GetAllObjects<LootableContainer> — seen in the IL). User steps for next build: Authors 29 (updated) + 1U re-run, save ALL, rebuild bundle, restart server.
- Interactables decompile session (2026-08-10, D:/SPT400_assembly): CONTAINER no-prompt ROOT CAUSE PROVEN — GetActionsClass.smethod_16 offers 'Search' ONLY when DoorState==Shut; the rip leaves containers at None -> empty actions -> no prompt (icebreaker Author 11 rebakes _doorState=2 for this exact reason; their extract_lootables.json carries the full field set + carve_lc_tail, and gen_static_containers.py regenerates the server file FROM THE BUILT BUNDLE because the SDK renumbers container ids at rebake — terminal's Author-27 authored ids matched server 217/224 on the DISK scenes, bundle-side unverified). NEW TerminalInteractables.cs: raid-start heal None->Shut on all containers (works with any bundle) + InstantDoorInteract config (default on) forcing interactWithoutAnimation. DOOR sound-no-motion: sound plays at SmoothDoorOpenCoroutine START, motion happens after via CurrentAngle setter (gates: parent!=null, !_keepRotation — both check out; InteractionShouldBeConfirmed=false in BaseLocalGame, no revert errors in log). Two suspects remain: player-animation-driven interaction path broken on backported map (discriminated by InstantDoorInteract) vs static-batched meshes in the BUILT bundle (editor-unstatic does not prove bundle state; needs narrow bundle scan of door renderers' m_StaticBatchInfo if instant-interact doesn't fix it).
- REGISTRY-WIRING REGRESSION CLASS (2026-08-10, cost one raid): wiring a component type into LocationScene registries (Author 29 full parity) hands it to ENGINE init paths that run BEFORE our runtime fill passes. Concrete case: BotsController.method_2 Inits every registry AIPlaceInfo; _isInited latched on the rip-EMPTY components, our later place.Init() no-op'd, TierEventLogic never subscribed OnPlayerEnter -> player walked TB2, zero events (worked pre-parity because nothing pre-inited them). Fix: TerminalAIPlaces now calls InfoLogicAllEnemy.Init directly (idempotent guard in TierEventLogic). GENERAL RULE: any runtime resurrection that fills rip-emptied components must either run before the engine's registry consumers or re-assert its wiring after — check who enumerates the registry type (grep GetAllObjects<T>) whenever Author 29 gains a new array.
- FULL CONTAINER REBAKE PIPELINE LANDED (2026-08-10): extract_terminal_lootables.py (verbatim icebreaker port — 1.0 surgery + carve_lc_tail are version-specific, not map-specific) -> terminal_lootables.json (256 LC + 5 groups, 0 failures; _doorState=2 x253 retail-authored; REAL axis variety vs the rip's guesses). Author 32 (TerminalLootables.cs) = Author-11 port + rotation fields in the whitelist (DoorAxis/DoorForward/angles — the lid-spin fix) + RemoveMonoBehavioursWithMissingScript (gunsafe valberg doors shipped Missing MonoScript). gen_terminal_static_containers.py regenerated staticContainers.json to all 256 (48 RUAF corpses + 10 gunsafes were silently dropped by the old string-carve for unknown tpls). 1.0-only tpl remaps (NONE in any installed mod): corpse tpls->PMC body, 67a0e494 name-split suitcase->Plastic suitcase/safe->Weapon box 5x5, safe_01->Safe, keycard suitcase->Weapon box 5x2 (user call). remap table mirrored client-side in TerminalInteractables — MUST stay in sync. LEVELSETTINGS TRUTH (extract_terminal_levelsettings.py): retail authors ambient+NVG hemispheres at ~0/black — map brightness lived in lightmaps the rip lost; heals synthesize (ApplyAmbient now fills NVG fields too, NvgAmbient config). DOOR NO-MOTION SOLVED: SpatialAudioSystem.ProcessInteractiveObjectOcclusion NRE killed SmoothDoorOpenCoroutine at its PlaySound opening step (TerminalAudioFixes armor = icebreaker recipe + the interactive-object overload icebreaker never covered). editor sequence pending: Author 32 -> save -> Author 29 -> save -> rebuild bundle.
- SPATIAL AUDIO PORT LANDED (2026-08-11): extract_terminal_spatial_audio.py (icebreaker port; layout surgeries version-specific not map-specific) recovered Terminal_Sound(level636): 104 SpatialAudioRooms / 345 portals / 158 AudioTriggerAreas / system + ambient classes, and Terminal_Scripts(level600): EnvironmentManager + 7 TriggerGroups + 64 IndoorTriggers. location_info = terminal_sound_info (resources.assets pid 46003, bake path AudioBakeData/terminal_sound.audiobakedata); occlusion preset BORROWED from icebreaker sidecar (GenericAudioOcclusionPreset is engine-generic, sharedassets397 pid 454 in both builds — terminal dump lacks that file). TerminalAcoustics.cs = IcebreakerAcoustics port, spatial tiers only (their Author-8 ambient-bed mixer NOT ported — TerminalSoundRig owns that role); staged via Patch_SpatialAudioInitSkip (staged -> real Initialize; else skip + occlusion airbags); env layer built + self-driven from the Terminal_Lights host. MISSING PIECE: terminal_sound.audiobakedata from the 1.0 client StreamingAssets/AudioBakeData -> plugin-data/acoustics (staging logs loudly until present; tier2 env works without it). LOUD AMBIENT HUNT: sound scene has MetalSqueaks*/MetalWindBlow* RPP emitters (rip lost their governing scripts — playOnAwake loops raw); F11 SoundProbeKey dumps playing sources near player, loudest first, to name offenders for a targeted clamp.
- BD RETAIL SCRIPT (user-specced 2026-08-11, TerminalBrainLayers zone table): Zone1BD1HangarAmbush11 = TB4 armory push (~8 BD, staggered boss+2 waves, Delay 18/30/22s) — ALWAYS hunt, they charge the player at the armory and brawl the scavs/RUAF there; Zone1BD1HangarBD11 = the keycard-4 (TB8, boss+3) — guard BD_hold/valbergs (native fix: 4 cloned interior spawn markers radius 3m at raid start, zone's 2 authored born positions rad:35 couldn't place a 4-group and scattered out the door; topper now arms on TB8 event as backstop); Zone1BDGateAmbush13 = 950s TIMER pair (no trigger — NOT TB5, which is scav storm into Zone1ScavPortStorm6) — guard a 24m box at their gate post. retail loop: gear confiscated -> player raids armory -> TB4 push arrives -> clear -> hangar keycard squad -> BD keycard. BotMax raised 30->40 (queue stalls).
- AMBIENT REBUILT FROM RETAIL (2026-08-11, supersedes the hand-tuned rig beds). KEY UNLOCK: terminal's dump lacks every sharedassets file, but the ICEBREAKER dump has them and is the SAME client build — pair each .assets with the globalgamemanagers from ITS OWN dump or every m_Script resolves to the wrong class (terminal's gg over icebreaker's sharedassets636 turned SoundBank into a Dissonance Resampler and looked exactly like layout drift). extract_terminal_soundbanks.py: 44 SoundBanks recovered (seagulls_one/metal_squeaks_*/rats/ships_rope/far_shoots...) with authored volumes + 366 clip names; SoundBank 1.0 drift is real but only past the prefix — 4.0 added _clipLiength and DistanceVarity gained ~4 floats + 2 ints, so scalars come from a hand-decoded byte prefix and clips from a self-validating PPtr carve (candidate must resolve to a real AudioClip). export_terminal_ambient_clips.py pulled the 299 clips the bundle lacked -> Assets/Terminal_Import/AmbientRetail (158MB); Author 26 now scans BOTH clip dirs. Client TerminalAcoustics.TryStageAmbient rebuilds banks + 1375 components under the single AmbientAudioSystem root, ROOT DEACTIVATED for the whole pass because the system's Awake harvests workers via GetComponentsInChildren (registry-order trap), system component added LAST, then Initialize (NetworkGame calls it online; offline nobody does). GOs already owned by TerminalSoundRig are skipped — first attach wins, two players on one GO = doubled audio. Config AmbientRetail; interim TryApplyAmbientAuthoring/AmbientGovernor auto-stand-down. F11 SoundProbe now always lists 2D/GLOBAL sources (the 60m distance cut hid the global sparrow loop that caused the whole hunt).
- BOSS-WAVE COMPOSITION, from the LIVE dump (G:/downloads/terminal/localloot/client_match_local_start.json — the authority; check it before reasoning from the decompile): a wave spawns BOSS + the SUPPORTS amounts ONLY. TB8 = blackDivision(impossible) + hard1/normal1/easy1 = 4 BD in the hangar, exactly as the user remembered. BossLocationSpawn.EscortCount (= BossEscortAmount + sum(Supports) = 6 for TB8) is NOT a spawn count — it only feeds CheckOnMax and spawn-point allocation; BossSpawnerClass.method_6 iterates SubDatas/Supports. ESCORT DELIVERY BUG (fixed by TerminalEscortFix): the boss honours wave.IgnoreMaxBots (Spawn skips CheckOnMax) but followers go through TryToSpawnInZoneAndDelay with forceSpawn=FALSE on every non-start wave -> capped/delayed queue -> boss lands alone and escorts trickle in minutes later or never. prefix on BossSpawnerClass.method_5/method_6 sets forceSpawn=true when the wave itself sets IgnoreMaxBots (retail intent, terminal-scoped). SPATIAL AUDIO was dead because an OLD installed ManimalIcebreaker build skipped SpatialAudioSystem.Initialize unconditionally on EVERY map (its own source has the IcebreakerLoaded gate, just never compiled) — terminal now self-drives Initialize and SUSPENDS foreign patches on it for that one call, so it no longer depends on another mod's build state. rooms staying untracked is also why every ambient player ran ungated ('same zone audio everywhere').
