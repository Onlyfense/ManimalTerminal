---
name: terminal-backport
description: "Terminal map backport kickoff — staging done, export done, host slot verified; follows the Icebreaker playbook"
metadata: 
  node_type: memory
  type: project
  originSessionId: d0a95716-c650-4f02-a997-b6b21f9e3c7e
  modified: 2026-07-26T20:48:32.034Z
---

Terminal (retail 1.0) backport started 2026-07-26, following [[map-backport-playbook]] (docs/MAP-BACKPORT-PLAYBOOK.md in ManimalIcebreaker).

- Staging: `C:\Users\peard\Desktop\TerminalLevels` — levels 600–637 + 650, all sharedassets/resources containers, 34.67 GB, size-verified against retail. Terminal levels carry NO .resS siblings (art all lives in shared containers).
- The map ships TWICE in build settings: block 1 = 600–637 (staged), block 2 = 651–689 — almost certainly the two port-event states (PortCutscene Intro/Attack/Ending scene names). Block 2 not staged.
- AssetRipper export: `TerminalLevels\AssetRipper_export_20260726_173044` — 39 scenes exported.
- Host slot: `Terminal` is a FIRST-CLASS property on SPT's Locations record, dormant stub (Locked, empty Scene, no exits/spawns), own Id "Terminal" + mongo `5704e5a4d2720bb45b8b4567`. No slot hijack or locale rebrand needed (unlike Icebreaker/Suburbs). Shoreline's vanilla transit SHO_TRANSIT_25 already targets it.
- PhysicsSetup extracted (scratchpad terminal_levelphysics.json): bounds centre (0,0,0) size 1567x327x1714, subdivisions 16. Bake it in the editor pass from day one — this is the component whose absence caused Icebreaker's transit-arrival PhysX 64K crash.
- SDK import convention: `Assets/Terminal_Import/Content/Locations/...`; bring scenes + art, NOT the export's Assets/Scripts stub folder (GUID-collides with the SDK's existing stubs).

- SDK import DONE: dependency-closure copy (scratchpad terminal_import.py) — 18,724 assets, 8.95 GB into `Assets/Terminal_Import` with `_import_manifest.txt`; wholesale copy was 56 GB, Icebreaker_Import precedent is 4 GB curated.
- AssetRipper script GUIDs are PER-RUN, not deterministic: terminal scenes all showed Missing Script. Fixed by guid rewrite (scratchpad terminal_script_rebind.py + terminal_package_rebind.py): 91 game classes -> SDK stubs, 8 package classes (Timeline/uGUI/TMP) -> Library/PackageCache guids, 164 files rewritten. Trap recorded in the playbook.
- ~243 classes still stubless (stub backlog, top: StaticDeferredDecal, MultisceneSharedOccluder, SmartGrip/LimbIK, StencilShadow, Culling*Light*, FlareLight/AreaLight, Handler* cutscene set). Next step: auto-generate stubs from SPT Managed typetrees (TypeTreeGenerator), the LevelPhysicsSettings.cs pattern industrialized. Unity.AI.Navigation package is NOT installed in the SDK (NavMeshModifier refs dangling) — install via Package Manager when navmesh authoring starts. Meta.XR.Audio refs: skip, acoustics get rebuilt anyway.

- Shader/material state (all tooling + audit in TerminalLevels\analysis): decals routed to Custom/Icebreaker Decal (175 mats), VP Solid -> Custom/Icebreaker Vert Paint Blend (11), puddles -> NEW Custom/Icebreaker Puddle shader (11 mats, fingerprinted by _RippleScale/_EditorWaterLevel; deterministic guid md5('manimal:IcebreakerPuddle.shader')). 4 SoftCutout Decal mats + fuel-slick look + UnderwaterSurface = open shader backlog.
- Shader audit (terminal_shader_audit.txt): 45/52 ripped shaders exist in 4.0.13 -> covered by porting the icebreaker raid-start rebind (Phase 7). CAVEAT: name-existence != rebindable — deferred decal/VP shaders exist in 4.0.13 but need the game's decal-system components to render, which is why the editor-side swaps were done anyway. 7 genuinely missing: ANGRYMESH PBR Rocks (terrain), Cloth x2, 3 explosion/particle FX, Hidden/Water/SurfaceBack — stand-in decisions when seen broken.

- Cutscenes: Terminal has TWO (intro at map start + a timed second one). Terminal_Cutscene_Content (level630) is a 1-GameObject holder — empty in retail too, not a rip loss. Real content: CutscenesSources scenes, exported to Terminal_Import/CutscenesSources/Scenes — PortCutscene_00_Intro (level631, 3680 GOs, 57 MBs), 01_Attack, 02_Ending_00. Intro references resources.assets (Timeline data lives there, AssetRipper husks it) — resources.assets/.resS/.resource re-copied into TerminalLevels for the extraction. Rebuild = the icebreaker chain: extract_cutscene_* + gen_cutscene_build_data (m_StopTime clip fingerprints) -> Author-17-style editor rebuild -> client driver ported with a MAP-START trigger (not a story trigger). User wants intro only for now.

- Intro cutscene chain DONE up to the editor rebuild (all scripts in TerminalLevels\analysis): pass1 (level631 wrappers — director has NULL playableAsset, the BSG CutscenePlayableDirector assigns it at runtime), pass2 graph with ROOT DISCOVERY via binding-key overlap (root = resources.assets pathID 45846 'Terminal_Area_PortCurscene_00_Intro_TimelineDirector' — BSG's typo; 100% stock Unity Timeline: 81 tracks/74 playables, no BSG track classes, WIP assembly not needed), 32/32 anims + 6/6 audio matched (m_StopTime fingerprints) and copied to Terminal_Import/CutsceneAnims (TRM_CS_ prefix), build data generated + bindings enriched 41/41 (40 Animators + 1 MonoBehaviour under CutsceneRoot/...). SDK Author 21 = TerminalCutsceneTimeline.cs (menu Terminal/Author 21): per-clip audio, per-track emitters via markerPids (TWO signal tracks — Author 17's all-emitters-on-every-track would double them), MarkerTrack skipped, case-insensitive director find. CONFIRMED WORKING 2026-07-26: user ran Author 21 + 1LD, cutscene previews in the Timeline window. Remaining: playback driver (Phase 7 client, MAP-START trigger, port of IcebreakerTimelineCutscene) + the second cutscene (01_Attack, timed — same chain, different scene seed) later.

- Lighting: 39/39 scenes have LightingData.asset; Author 1LD generalized (resolves from scene path, serves both imports) — restores probes/ambient/reflection. Per the user, lighting is ALL DYNAMIC lights, same as icebreaker — the lightmap-bake path (Lighting 0/2 tools) went nowhere on icebreaker and is NOT wanted for terminal. Real lighting work = client runtime (ambient fill, lamp driving, culling-light restore).

**Why:** next session should resume at the stub generator without re-deriving any of the above.

**How to apply:** read the playbook first; extractors in ManimalIcebreaker/analysis are parameterized by LEVELS_DIR + LEVELS range (600–638 for Terminal).
- Attack cutscene "empty shot" postmortem (2026-08-09, frames 1374-1500 / 22.9-25.0s): TWO separate causes. (1) Scav04's shot-hold clip (clipPid 3300 'Recorded (5)', 0.52s) never matched — constant hold clips have m_StopTime EXACTLY 0.0 on both retail and export sides, and the matcher's `(stop or 999)` falsy-zero fallback turned the perfect match into the worst score. When his run anim (Recorded (25)) ended at 22.9s the empty clip snapped PosAdjust back to origin, teleporting him 17m out of frame for exactly the shot window. Fixed in attack_match_anims.py + match_terminal_cutscene_anims.py (explicit None checks); 146/146 now match; same fix applies to any future map. Zero-stoptime "unmatched Recorded" clips are NEVER benign — they are the hold poses. (2) The "vehicle" he shoots over is a Uaz_military prop that lives in Terminal_LIGHT.unity (BSG parents props with light rigs into the light scene — checkpoint UAZ is under SBG_Terminal_Light/OO/LIGHT_with_no_Culling/Terminal_Area_01_CutScene_Outdoor/LIGHT/OFF/). It is intact in the SDK and in the bundle; it only looked missing because Terminal_Light.unity was not loaded during Timeline preview. Lesson: when previewing cutscenes, additively load Terminal_Light. Known remaining cosmetic: Utes 'Take 001' deploy anim has AssetRipper hash-only curve paths (path_0x..) so it binds nothing — mount stays static during the 3.4-10.1s load beat; fixable later by CRC32-resolving paths against the Utes subtree.
- SOUND rig orchestrator REWORKED to the icebreaker AmbientAudio model (2026-08-09, builds clean): the ambient player/spline stack is ALIVE in 4.0 Assembly-CSharp (verified per-class against the DLL) — OneShot/AmbientSoundPlayer, AmbientSoundPlayerGroup, AmbientPlayerGroupSplineEmitter, SplineEmitterPathMover, SoundPlayerSplineTrigger, AmbientSplineEmitterController, BezierSpline, EFT.SoundBank. terminal_sound_components_spec.py emits plugin-data/sound_rig_components.json (79 components, icebreaker-style $AudioClip/$asset:SoundBank/$ref tags) + sound_rig_banks.json (19 banks/40 clips). TerminalSoundRig.cs: inactive-root two-pass restore (players first — [RequireComponent] on abstract BaseAmbientSoundPlayer), reflection SoundBank rebuild (Environments[0][0], HasEnvironment off), ClampForever on all restored curves (looping rolloff = map-wide audio, icebreaker lesson), spread is INVERTED 0-1 (Lerp(180,0,v)). Phase gating stays ours (cutscene event filters dead): between/after group SetActive + BSG Play() via reflection with direct-clip fallback. Firefight sim + sirens unchanged (director classes dead). NEW post-attack door beat: door_Terminal_Area_01_CutScene_00000 unlocks (Locked->Shut + KeyId cleared) + AdditionalSounds plays — DoorOpenSound = entity_cardlock_doorunlock (exported from sharedassets54:1363; the rig's ref misparsed to a GameObject pid), foley +1.2s; VSRF voice line is dead-localized-player-fed, stays silent. NEW camera shake: TerminalAttackCutscene scans its own rebuilt timeline for CameraShake_01 SignalEmitters (7) and applies decaying perlin wobble in DriveCamera (ExplosionShaker chain dead). csproj now references Unity.Timeline. Still not ported: EnvironmentSwitcher spatial rooms (blocked on SpatialAudioSystem being skipped), mixer fade handlers (subsumed by group deactivation). User editor steps: Author 26 (now 41 clips incl the door unlock), save Design_Stuff, rebuild bundle.
- Door foley RESOLVED per-family (2026-08-10): the doors json's loose parse had misaligned sound refs AND the WIP tree has its own Door drift (14/60 fail, rest read empty) — anchor-carve found the real refs but the RETAIL INSTALL UPDATED SINCE THE RIP (current ggm resolves level script pptrs to NOTHING — always pair level files with TerminalLevels' own globalgamemanagers.assets; sharedassets5's pids shifted non-uniformly, no clean delta). Exact pids unrecoverable -> semantic family mapping in extract_terminal_door_sounds.py: 60 doors -> plastic(13)/wood(6)/grate(15)/metal1(26) by prefab name, arrays emitted PRE-BUCKETED with sibling-ordinal (name~k) paths (60/60 match terminal_doors.json). Retail door clip families found by name sweep across all 291 level externals (terminal_door_clip_inventory.json): door_plastic_*, door_wood_modern/old_*, door_grate_*+barbwire2, metal_door_1/2_* incl ruchka handle clips; 29 clips exported to Terminal_Import/DoorSounds. Author 1R patched: uses bucketed arrays VERBATIM (old substring re-bucketing dropped barbwire2/ruchka), per-door HitClip/BreachSound, DoorSounds dir first in WavDirs. BONUS: vsrf_door_open_voice_ru/_eng found in sharedassets628 (the "unrecoverable" localized door voice) — both exported to SoundRig, door beat now plays unlock (0s) + foley (1.2s) + eng voice (2.2s). User editor steps: re-run Author 26 (SoundRig gained 3 clips: door unlock + 2 voices), re-run Author 1R, save scenes, rebuild bundle.
- LIVE 1.0 DUMPS integrated (2026-08-10, from G:/downloads/terminal): 8 client/match/local/start responses (data.locationLoot = the FULL live Location object) + 23 bot-generate dumps (1086 bots). terminal_live_loot.py -> db/staticContainers.json (224/231 containers; 3 1.0-only container tpls remapped to 4.0 pool-mates by content overlap, scene binding is by Id so tpl only picks the pool), db/staticLoot.json (22 pools), db/looseLoot.json (47 spawnpoints + 383 FORCED - dead-body/preset/vog battlefield dressing is IsAlwaysSpawn in live; mean 414 loose/raid); 78 unique 1.0-only tpls trimmed (~330 instances, incl quest notes). terminal_live_base.py -> base.json 3KB->135KB: 109 SpawnPointParams (104 Savage + 5 Pmc, east/west infils), 68 BossLocationSpawn (assault 40, blackDivision 15, vsRF 8, civilian 3, vsRFSniper 1, bossTagilla 1 - BD/VSRF roles resolve via the WTT BlackDiv/MoreBots deps), 40 waves (all native assault), 5 exits (live ships Chance 0 = transit-gated; bumped to 100: Unity_free_exit/Sniper_exit/Nakatani_stairs free, Sandbox_VExit TransferItem, Scav_coop_exit ScavCooperation), BotLocationModifier + full bot count/timing (BotMax 30, BotStart 420, BotStop 2800). Preserved overrides: Id/_Id/Name/Locked/Enabled/Scene/EscapeTimeLimit/DisabledForScav/GenerateLocalLootCache. Server builds; all 4 db files deploy to SPT/user/mods/ManimalTerminal/db. FUTURE: bot dumps carry full live BD/VSRF loadouts (Inventory trees) - loadout enrichment for BlackDiv bot types not yet mined; navmesh/AI bake still pending for bots to actually path.
- Boss role remap finalized (2026-08-10, user call): civilian rows DROPPED (3); blackDivision -> blackDivAssault (TacticalToaster/BlackDiv enum 848421); vsRF -> ruafRifleman (848400) and vsRFSniper -> ruafMarksman (848404) from TacticalToaster/RUAFComeHome — NEW DEPENDENCY added to TerminalMod.cs: com.ruafcomehome.tacticaltoaster >=1.1.0 (guid from repo Server/Mod.cs, v1.1.2, needs morebotsapi+commonlib+contentbackport itself). Other registered roles available if wanted later: blackDivLead/Breacher/Support, bossWedge, blackDivIb; ruafRiflemanSenior/Autorifleman/Grenadier/Machinegunner, remnantRifleman. Remap lives in terminal_live_base.py (covers BossName, BossEscortType, nested Supports[]). base.json now: 65 boss spawns (assault 40, blackDivAssault 15, ruafRifleman 8, ruafMarksman 1, bossTagilla 1). Server builds clean.
- Hostility matrix authored (2026-08-10, user calls): TERMINAL-ONLY by construction — lives in our base.json BotLocationModifier.AdditionalHostilitySettings (7 entries from live, role-remapped), other maps' RUAF untouched. ruafRifleman/ruafMarksman: Bear/Usec/SavagePlayerBehaviour explicitly 'Neutral' (live shipped vsRF with NO player keys = neutral by default; made explicit — neutral bots retaliate when shot, the requested behaviour) + AlwaysEnemies vs blackDivAssault/assault/assaultGroup. blackDivAssault: 100% ChancedEnemies vs both PMC factions (live values) + AlwaysEnemies vs RUAF/scavs. Scavs: enemies with PMCs/BD/RUAF. civilian stripped from all lists. RUAFComeHome's own plugin does NOT hard-set hostility (BotsControllerInitPatch only inits checkpoint managers) so location settings govern. Verified in generated base.json.
- Loot tpl filter fixed + container Id rebake (2026-08-10): RULE (user call) — before dropping a tpl, check the installed mods' CustomItems configs (WTT-ContentBackport carries most 1.0 items). terminal_live_loot.py allow-set = vanilla + 1564 modded tpls (Armory 822, ContentBackport 593, ...); dropped fell 78->8 tpls (only true-missing quest notes remain), forced spawns 383->390. Checkpoint keycard 6866ad3853330f9b83064cf9 (the Administration keycard-door key, ContentBackport-provided) FORCED into the design-stuff suitcase via staticForced {containerId: container_Terminal_Design_Stuff_00186, itemTpl} (bigmap-verified shape). Armory key 68a4b745b63e97cc5a056a07 restored: fixed forced spawn (206.7,-58.6,-237.1), 3-position group spawn arc_key_alt_1 (incl the Corpse_RUAF_06 point at 123.7,-60.4,-403.2), chanced Substation container. CRITICAL SCENE GAP FOUND: the rip left EVERY LootableContainer Id EMPTY (no server binding = no container loot at all). extract_terminal_containers.py: typetree reads Id empty (drift) but raw bytes carry Id + Template tpl as strings in field order -> string-carve; 255/256 recovered (the 2-char 'n1' id lost to carve threshold). NEW Author 27 (TerminalContainerIds.cs): position-matched Id+Template rebake. User steps: Author 27 on open Terminal scenes (Design_Stuff has 255), save, rebuild bundle.
- Loot no-spawn postmortem (2026-08-10): TWO stacked causes. (1) looseLoot.json: my synthetic merged-variant item ids were decimal strings — SPT's MongoId converter hard-rejects non-24-hex and jsonUtil SWALLOWS the exception into a silent null, so the mod's 'missing' warning was misleading (the else branch catches both not-exists and null-parse). Fixed: md5(lid:ck:i:j)[:24] hex ids; ALL THREE db files now verified against SPT's REAL serializer via a scratchpad net9 console app that constructs SPT's JsonUtil with its own SptJsonConverterRegistrator (LootTest — keep this trick: it surfaces the exact JSON path SPT would silently swallow). (2) the raid was played against the stale factory fallback (ids dont match the map = zero loot). Container loot additionally needs Author 27 scene ids IN the built bundle. Debug rule: [Terminal] boot lines are the loot health check — want 'container loot pools loaded (22)', 'authored loose loot loaded', 'container set loaded from bundle scan'. NEW Author 28 (TerminalFadeCanvas.cs): authors the UI_FadeToBlack rigs scene-side in both cutscene scenes (overlay@9999, 1920x1080 scaler, black rgb + full-stretch, raycast off) — the rip broke render mode/scaler/color; runtime driver fixes stay as fallback. User steps: Author 28 on both cutscene scenes, save, rebuild bundle; restart server and check the three loot lines.
- Fade anim curves fixed (2026-08-10): the three fade clips (intro TRM_CS_3329_Recorded_1 62s master fade; attack TRM_CSA_3319/3450 in/out) were dead TWO ways — AssetRipper hash-placeholder attribute (script_0x1222D899 -> m_Color.a) AND a synthetic UI-assembly script guid on the curves (3cf5a444... -> real UnityEngine.UI.Image fe87c0e1cc204ed48ad3b37840f39efc; unity binds MonoBehaviour float curves by path+classID+SCRIPT GUID+attribute, so either mismatch kills the bind). Patched in place — no Author re-run needed (timeline references the same .anim asset paths); unity reimports on focus. These were the only script_0x casualties in the whole CutsceneAnims set. Diagnostic chain for 'scrubbing does nothing': binding scenePath ok -> Animator present -> anim attribute/guid vs scene component. Fade rig at rest is OPAQUE now that the canvas works — editor QoL: toggle UI_FadeToBlack off while composing (driver SetActive(true)s it in game regardless).
- Fade-rig rest-alpha trap (2026-08-10): Author 28's canvas fix made BOTH scenes' fade overlays functional, and the authored rest pose is alpha=1 — so with both cutscene scenes open in the editor, the scene you are NOT scrubbing is a working opaque ScreenSpaceOverlay@9999 black sheet over the entire Game view (looked like 'camera renders nothing'; disabling the CURRENT scene's fade changes nothing because it is the OTHER scene's copy). Author 28 now sets rest alpha 0 — safe in-game because the intro master fade drives alpha across its whole 62s, the attack fade-in clip starts at t=0 with value 1, and the drivers' own OnGUI fade covers scene entry/exit. Debug rule: a fully-black Game view with working overlay canvases = check EVERY loaded scene's overlay canvases first, not the camera.
- Night clock + lootability + door-open (2026-08-10): NEW TerminalRaidClock — RaidStartHour config (default 22.0, -1 off) anchors the raid clock at raid start then lets it tick; resets BOTH GameWorld.GameDateTime and GClass4.CurrentTime's GameDateTime (can be distinct instances; sky follows via TickSkyTime). Containers not lootable = (a) that raid ran the stale factory-fallback loot, (b) terminal scenes have NO LocationScene components — the game enumerates scene content through LocationScene arrays, not FindObjectsOfType (icebreaker Author 5/11 lesson). NEW Author 29 (TerminalLocationScenes.cs): creates LocationScene per scene, wires LootableContainers + WorldInteractiveObjects, empty-sweeps other arrays (lamps/audio deliberately NOT wired — our drivers own those). Cutscene door didnt open: retail authors Snap = Locked|Shut ONLY (the EVENT opens it, not players) — door beat widens Snap to include Open + unlocks (Locked->Shut, KeyId cleared) — NO auto-open (user call: the player opens it themselves; the Snap mask missing Open was why interaction refused). Compiler-verified APIs: GameDateTime.Reset(DateTime), Door.Snap, InteractionResult. NAVMESH RECOVERED (user pushback was right): NO map keeps NavMeshData in its LEVEL files — icebreaker's levels are equally empty and every NavMeshSettings.m_NavMeshData pointer ships NULL; the mesh lives in a SHAREDASSETS. Terminal's is sharedassets635.assets (Terminal_AI sibling) pid 2 'NavMesh-Global' 4.7MB — found by scanning all 291 level-external files. Exported via headless AssetRipper -> Terminal_Import/NavMeshData/NavMesh-Global.asset (9.7MB yaml; + NavMesh-NavMesh.asset from sharedassets1, probably another map's). NEW Author 30 (TerminalNavMeshRestore.cs, icebreaker Author 23 port): assign to ACTIVE scene (Terminal_Scripts) NavMeshSettings, SAVE, rebuild. Meshless = floating stacked-bot piles with zero editor symptom. AI-bake port (task #8) still pending for bot POINTS. User steps: Author 29 with all scenes open, save, rebuild bundle.
- NavMesh asset finalized (2026-08-10): the headless AssetRipper run exported BINARY .assets (icebreaker's July run produced YAML — settings drift); binary imports questionably and, worse, the retail mesh ships agentTypeID -334000983 (BSG custom) which has no agent entry in the SDK project = NO editor preview and default NavMeshAgent queries (SPT bots, typeID 0) MISS the surface. convert_terminal_navmesh.py: UnityPy-parses the binary and re-emits icebreaker-shaped YAML with agentTypeID -> 0 in both m_AgentTypeID and m_NavMeshBuildSettings (icebreaker's working mesh is typeID 0 — proven treatment). NavMesh-Global: 972 tiles, 0 offmesh links, 9.5MB yaml. User: Unity refresh, re-run Author 30 Assign NavMesh-Global (previous assign may have bound the binary import), save Terminal_Scripts, rebuild; preview needs the AI Navigation overlay's Show NavMesh checked.
- Navmesh CONFIRMED in-editor (2026-08-10): user assigned NavMesh-Global via Author 30, cyan preview draws. Retail pathing surface fully recovered.
- AI BAKE PORTED (2026-08-10, builds clean): extract_terminal_aibake.py + extract_terminal_aiplaces.py (param-patched icebreaker scripts, level635) — EVERY class byte-exact (same 1.0 drift surgeries hold): 2.5MB AICoversData, 1.6MB voxel grid, 705 mines, 30 core points, 49 NavMeshDoorLinks, AIPatrolsData. NEW enrich_terminal_patrols.py recreates the lost icebreaker patrol post-pass: 1568 PatrolPoints + 35 PatrolWays (4.0-tree byte-exact) + 35 BotZone memberships by raw ref-scan (BotZone drifted). Client: TerminalAIBake.cs (icebreaker port; TryFill in AICoversData.RestoreData prefix + finalizer swallow, patrol navmesh snap with the triangulation + hit-rate guards, MidOpen/MidClose door-link recompute, carver rebuild) + TerminalAIPlaces.cs (BotsController.Init postfix; 17 trigger boxes + 16 TierEventLogic — human-gated EventRaise reimpl; NO group-size class on terminal; no crew bridge needed, BSG BossSpawnScenario subscribes natively). KEY: 55/65 live boss waves are botEvent-gated with TriggerIds matching the place EventNames (TB1-TB8, T0-T5, 'Waaagh') — without places the spawn choreography never fires. Sidecars: plugin-data/aibake/terminal_aibake.json (31MB) + plugin-data/aiplaces/. Scrub list AI entries REMOVED (TryFill + RaidFixes null-heal own the holders now). Config RetailAIBake (default on). csproj += UnityEngine.PhysicsModule + AIModule. Remaining for bots: fika-teammate gating in TierEventLogic (IsYourPlayer solo-only), first live bot raid validation.
- AI bake first-raid autopsy (2026-08-10): fill itself LANDED (11956 covers, 7732 ways, 28900 voxels, 1568 patrols snapped 278/0.15m worst) but RestoreData threw KeyNotFound '0' -> half-restored holders NRE'd through BotsController.Init into the matching-screen popup. ROOT CAUSE: retail's own bake carries ONE orphan GroupPointWay (Dist 0, IdTarget 0; point ids start at 3) — 1.0's RestoreData tolerates it, 4.0's dictionary indexer throws. Referenced by nothing. FIXES: TryFill drops ways targeting nonexistent point ids; FillFields skips retailExtra_* bookkeeping keys (705x warn spam); finalizer now clears Points/Ways/Pathes to coherent-empty on swallowed failure (half-state was the popup). CORRECTION (second autopsy): the 43k dangling NeighbourhoodsWaysIds are NOT tolerated — icebreaker's data has ZERO missing refs; 4.0's RestoreData return-false's on the FIRST miss (silent unity LogError only), InitForGamePoints never runs, Childs stays null, CachePoints NREs. Decompiled source (D:/SPT400_assembly) was the breakthrough — retail terminal serialized only 7732 of 51246 referenced ways (1.0 restore is tolerant, 4.0 isn't). Fix: TryFill strips dangling neighborhood refs JSON-side before the fill (id-space reconcile, three passes). Also audited the rest of the restore chain: GetCorePoint = FirstOrDefault (tolerant), danger places empty, voxel point refs 0 missing. AICorePoint id lives in _id (not Id) — fine.
- Icebreaker bot-hardening set ported (2026-08-10, TerminalBotFixes.cs, builds clean): Patch_CoversCache (CachePoints finalizer — degrades to lazy cache), Patch_BotDoorsRefresh (BotDoorsController.RefreshData swallow), Patch_PlaySoundAirbag (bot-hearing dangling-ref NREs, rate-limited), Patch_DoorTriggerEmit (WorldInteractiveObject.method_3 — dead quest-trigger singleton NREs after EVERY door swing on backported maps; vanilla maps keep theirs), Patch_BotActivationStepwise (THE icebreaker scar: mirrored 49-step method_10 with per-step guards + named failure logs — one broken subsystem no longer silently statues the bot; skips original, BotState=Active mid-list as BSG authored), TerminalStuckBots watchdog REMOVED (user call — not wanted on terminal; icebreaker-specific selection-behavior remedy). DissonanceVoip ref dropped with it. NOT ported: IceCrewJobs bigbrain idle layer (fed by icebreaker's crew system; only needed under SAIN — if SAIN idle-statue shows in testing, adapt it with a zone-patrol job source) and synth cover/zone generation (terminal has the retail bake).
- Bot-raid triage round 2 (2026-08-10): bots WERE generated (server log: 180 incl blackDivAssault/ruafRifleman/bossTagilla — remap + freecam-tripped botEvent triggers all working) but never PLACED: BotsController takes its BotZone[] from the LocationScene registries and Author 29 v1's 'safe minimal set' left BotZones/ExfiltrationPoints/etc EMPTY. Author 29 now wires FULL icebreaker-Author-5 parity (zones, spawn markers, exfils, door links, places, lamps, audio + containers + interactives). Sky stayed daytime: single-shot clock postfix only caught GameWorld's clock ('1 clock(s)'); GClass4's (the one TickSkyTime reads) wasn't up yet — RaidClock is now a 20s retry host anchoring BOTH and reporting which landed. Doors 'sound but no motion' = classic STATIC BATCHING symptom — TerminalUnstatic (Author 1U) must run AFTER the latest Author 1R re-run and be in the built bundle. Containers no-prompt = LocationScene registries missing from the built bundle (game binds via LocationScene.GetAllObjects<LootableContainer> — seen in the IL). User steps for next build: Authors 29 (updated) + 1U re-run, save ALL, rebuild bundle, restart server.
- Interactables decompile session (2026-08-10, D:/SPT400_assembly): CONTAINER no-prompt ROOT CAUSE PROVEN — GetActionsClass.smethod_16 offers 'Search' ONLY when DoorState==Shut; the rip leaves containers at None -> empty actions -> no prompt (icebreaker Author 11 rebakes _doorState=2 for this exact reason; their extract_lootables.json carries the full field set + carve_lc_tail, and gen_static_containers.py regenerates the server file FROM THE BUILT BUNDLE because the SDK renumbers container ids at rebake — terminal's Author-27 authored ids matched server 217/224 on the DISK scenes, bundle-side unverified). NEW TerminalInteractables.cs: raid-start heal None->Shut on all containers (works with any bundle) + InstantDoorInteract config (default on) forcing interactWithoutAnimation. DOOR sound-no-motion: sound plays at SmoothDoorOpenCoroutine START, motion happens after via CurrentAngle setter (gates: parent!=null, !_keepRotation — both check out; InteractionShouldBeConfirmed=false in BaseLocalGame, no revert errors in log). Two suspects remain: player-animation-driven interaction path broken on backported map (discriminated by InstantDoorInteract) vs static-batched meshes in the BUILT bundle (editor-unstatic does not prove bundle state; needs narrow bundle scan of door renderers' m_StaticBatchInfo if instant-interact doesn't fix it).
- REGISTRY-WIRING REGRESSION CLASS (2026-08-10, cost one raid): wiring a component type into LocationScene registries (Author 29 full parity) hands it to ENGINE init paths that run BEFORE our runtime fill passes. Concrete case: BotsController.method_2 Inits every registry AIPlaceInfo; _isInited latched on the rip-EMPTY components, our later place.Init() no-op'd, TierEventLogic never subscribed OnPlayerEnter -> player walked TB2, zero events (worked pre-parity because nothing pre-inited them). Fix: TerminalAIPlaces now calls InfoLogicAllEnemy.Init directly (idempotent guard in TierEventLogic). GENERAL RULE: any runtime resurrection that fills rip-emptied components must either run before the engine's registry consumers or re-assert its wiring after — check who enumerates the registry type (grep GetAllObjects<T>) whenever Author 29 gains a new array.
- FULL CONTAINER REBAKE PIPELINE LANDED (2026-08-10): extract_terminal_lootables.py (verbatim icebreaker port — 1.0 surgery + carve_lc_tail are version-specific, not map-specific) -> terminal_lootables.json (256 LC + 5 groups, 0 failures; _doorState=2 x253 retail-authored; REAL axis variety vs the rip's guesses). Author 32 (TerminalLootables.cs) = Author-11 port + rotation fields in the whitelist (DoorAxis/DoorForward/angles — the lid-spin fix) + RemoveMonoBehavioursWithMissingScript (gunsafe valberg doors shipped Missing MonoScript). gen_terminal_static_containers.py regenerated staticContainers.json to all 256 (48 RUAF corpses + 10 gunsafes were silently dropped by the old string-carve for unknown tpls). 1.0-only tpl remaps (NONE in any installed mod): corpse tpls->PMC body, 67a0e494 name-split suitcase->Plastic suitcase/safe->Weapon box 5x5, safe_01->Safe, keycard suitcase->Weapon box 5x2 (user call). remap table mirrored client-side in TerminalInteractables — MUST stay in sync. LEVELSETTINGS TRUTH (extract_terminal_levelsettings.py): retail authors ambient+NVG hemispheres at ~0/black — map brightness lived in lightmaps the rip lost; heals synthesize (ApplyAmbient now fills NVG fields too, NvgAmbient config). DOOR NO-MOTION SOLVED: SpatialAudioSystem.ProcessInteractiveObjectOcclusion NRE killed SmoothDoorOpenCoroutine at its PlaySound opening step (TerminalAudioFixes armor = icebreaker recipe + the interactive-object overload icebreaker never covered). editor sequence pending: Author 32 -> save -> Author 29 -> save -> rebuild bundle.
- SPATIAL AUDIO PORT LANDED (2026-08-11): extract_terminal_spatial_audio.py (icebreaker port; layout surgeries version-specific not map-specific) recovered Terminal_Sound(level636): 104 SpatialAudioRooms / 345 portals / 158 AudioTriggerAreas / system + ambient classes, and Terminal_Scripts(level600): EnvironmentManager + 7 TriggerGroups + 64 IndoorTriggers. location_info = terminal_sound_info (resources.assets pid 46003, bake path AudioBakeData/terminal_sound.audiobakedata); occlusion preset BORROWED from icebreaker sidecar (GenericAudioOcclusionPreset is engine-generic, sharedassets397 pid 454 in both builds — terminal dump lacks that file). TerminalAcoustics.cs = IcebreakerAcoustics port, spatial tiers only (their Author-8 ambient-bed mixer NOT ported — TerminalSoundRig owns that role); staged via Patch_SpatialAudioInitSkip (staged -> real Initialize; else skip + occlusion airbags); env layer built + self-driven from the Terminal_Lights host. MISSING PIECE: terminal_sound.audiobakedata from the 1.0 client StreamingAssets/AudioBakeData -> plugin-data/acoustics (staging logs loudly until present; tier2 env works without it). LOUD AMBIENT HUNT: sound scene has MetalSqueaks*/MetalWindBlow* RPP emitters (rip lost their governing scripts — playOnAwake loops raw); F11 SoundProbeKey dumps playing sources near player, loudest first, to name offenders for a targeted clamp.
- BD RETAIL SCRIPT (user-specced 2026-08-11, TerminalBrainLayers zone table): Zone1BD1HangarAmbush11 = TB4 armory push (~8 BD, staggered boss+2 waves, Delay 18/30/22s) — ALWAYS hunt, they charge the player at the armory and brawl the scavs/RUAF there; Zone1BD1HangarBD11 = the keycard-4 (TB8, boss+3) — guard BD_hold/valbergs (native fix: 4 cloned interior spawn markers radius 3m at raid start, zone's 2 authored born positions rad:35 couldn't place a 4-group and scattered out the door; topper now arms on TB8 event as backstop); Zone1BDGateAmbush13 = 950s TIMER pair (no trigger — NOT TB5, which is scav storm into Zone1ScavPortStorm6) — guard a 24m box at their gate post. retail loop: gear confiscated -> player raids armory -> TB4 push arrives -> clear -> hangar keycard squad -> BD keycard. BotMax raised 30->40 (queue stalls).
- AMBIENT REBUILT FROM RETAIL (2026-08-11, supersedes the hand-tuned rig beds). KEY UNLOCK: terminal's dump lacks every sharedassets file, but the ICEBREAKER dump has them and is the SAME client build — pair each .assets with the globalgamemanagers from ITS OWN dump or every m_Script resolves to the wrong class (terminal's gg over icebreaker's sharedassets636 turned SoundBank into a Dissonance Resampler and looked exactly like layout drift). extract_terminal_soundbanks.py: 44 SoundBanks recovered (seagulls_one/metal_squeaks_*/rats/ships_rope/far_shoots...) with authored volumes + 366 clip names; SoundBank 1.0 drift is real but only past the prefix — 4.0 added _clipLiength and DistanceVarity gained ~4 floats + 2 ints, so scalars come from a hand-decoded byte prefix and clips from a self-validating PPtr carve (candidate must resolve to a real AudioClip). export_terminal_ambient_clips.py pulled the 299 clips the bundle lacked -> Assets/Terminal_Import/AmbientRetail (158MB); Author 26 now scans BOTH clip dirs. Client TerminalAcoustics.TryStageAmbient rebuilds banks + 1375 components under the single AmbientAudioSystem root, ROOT DEACTIVATED for the whole pass because the system's Awake harvests workers via GetComponentsInChildren (registry-order trap), system component added LAST, then Initialize (NetworkGame calls it online; offline nobody does). GOs already owned by TerminalSoundRig are skipped — first attach wins, two players on one GO = doubled audio. Config AmbientRetail; interim TryApplyAmbientAuthoring/AmbientGovernor auto-stand-down. F11 SoundProbe now always lists 2D/GLOBAL sources (the 60m distance cut hid the global sparrow loop that caused the whole hunt).
- BOSS-WAVE COMPOSITION, from the LIVE dump (G:/downloads/terminal/localloot/client_match_local_start.json — the authority; check it before reasoning from the decompile): a wave spawns BOSS + the SUPPORTS amounts ONLY. TB8 = blackDivision(impossible) + hard1/normal1/easy1 = 4 BD in the hangar, exactly as the user remembered. BossLocationSpawn.EscortCount (= BossEscortAmount + sum(Supports) = 6 for TB8) is NOT a spawn count — it only feeds CheckOnMax and spawn-point allocation; BossSpawnerClass.method_6 iterates SubDatas/Supports. ESCORT DELIVERY BUG (fixed by TerminalEscortFix): the boss honours wave.IgnoreMaxBots (Spawn skips CheckOnMax) but followers go through TryToSpawnInZoneAndDelay with forceSpawn=FALSE on every non-start wave -> capped/delayed queue -> boss lands alone and escorts trickle in minutes later or never. prefix on BossSpawnerClass.method_5/method_6 sets forceSpawn=true when the wave itself sets IgnoreMaxBots (retail intent, terminal-scoped). SPATIAL AUDIO was dead because an OLD installed ManimalIcebreaker build skipped SpatialAudioSystem.Initialize unconditionally on EVERY map (its own source has the IcebreakerLoaded gate, just never compiled) — terminal now self-drives Initialize and SUSPENDS foreign patches on it for that one call, so it no longer depends on another mod's build state. rooms staying untracked is also why every ambient player ran ungated ('same zone audio everywhere').
- ACOUSTIC VOLUMES MUST BE TRIGGERS ON THE TRIGGERS LAYER (user diagnosis 2026-08-11, remembered from icebreaker): SpatialAudioPortals sit IN doorways by definition, so a rebuilt portal whose collider stayed SOLID (and on a raycastable layer) stands between the player and the door — the door probes perfectly healthy (Shut, unlocked, Interactive layer, collider on, Operatable, noInteractions=false) and STILL has no prompt, because the interaction ray never reaches it. BSG's own ServerRoomColliderArea.Awake does layer=TriggersLayer; portals/rooms never got it in our rebuild. MakeAcousticVolume now forces isTrigger + Triggers layer on every portal/area/room and counts what it fixed. F9 InteractProbe (TerminalInteractables.ProbeHost) dumps interactive state AND the crosshair raycast's first hits — that's the instrument that isolates 'object is broken' from 'something is in front of it'. SPARROW, FINAL ANSWER: amb_sparrow_in_garbage_02 is in ZERO of the 44 retail banks — it was OUR hand-built rig data, first clip in 6 sound_rig.json players (3 of them LOOPING at 0.6-0.7 volume). banks were only half the problem. SKY: rip materials carry a baked renderQueue override that SURVIVES a shader swap — additive TOD sun/moon/space stuck at Geometry render as opaque black-backed quads; renderQueue=-1 after rebinding restores shader default. ENV EXPOSURE: EnvironmentManager drives PrismExposureOffset (retail outdoor 0.23) against baked lighting we don't have — EnvironmentExposure config defaults to 0.
- TIME/SKY/WEATHER, SOLVED END TO END (2026-08-11). why 'our manual time setting' never worked despite GameDateTime.Reset() succeeding every raid: (1) TOD_Sky.method_18 — the sun solver — opens with Components.LightTransform.rotation, and TOD_Components.Init only sets LightTransform when its `Light` GO ref survived. the rip dropped it, so the solver threw on its FIRST LINE every frame and the sky never recomputed at all, sitting at the scene's authored Cycle.Hour=13.0. (2) TOD_Time.Start seeds the cycle from EFTDateTimeClass.Now (REAL-WORLD time) and re-advances it every frame, stomping our per-second Cycle writes — which is exactly why the map was dark when the user played at night IRL and bright when they played in the afternoon. fix: heal the light ref, wire TOD_Time.GameDateTime to the raid clock, then DISABLE TOD_Time so TickSkyTime owns the cycle. (3) ToDController — the thing that darkens Atmosphere.Brightness/ScatteringBrightness as the sun drops — lives on WeatherController, which the rip left absent, so the atmosphere kept daytime constants regardless of sun height: TerminalLights.TickNightSky is the substitute (SkyNightStrength config), pinned to night 21:00-06:00 since retail only opens the map then. WEATHER/RAIN: EFT renders rain via WeatherController -> RainController/RainFallDrops/RainSplashController, all husks here ('no WeatherController' in the log). terminal level600 carries the full authored tree, so extract_terminal_weather.py (icebreaker port) + TerminalWeather.TryStage rebuild it — TOD_* deliberately EXCLUDED (adding TOD_Sky auto-adds blank RequireComponent siblings that win the GetComponent race, icebreaker's documented trap). weather values then set the TimeWeatherChanger way: WeatherDebug.isEnabled + rain/clouds/fog/wind/thunder. configs: WeatherStack, ForceWeather, Weather*, SkyNightStrength.
- SKY SOLVED, THE LAST MILE (2026-08-15). the unconditional TOD probe (own tick counter — a %300 check inside a %10-frame caller can never fire, which is why the first probe logged nothing) produced the decisive numbers: fed a straight 22:00, the solver computes sunDir.y=+0.115 / IsDay=True — sun ~7 deg ABOVE the horizon with retail's authored lat=46/UTC=7.24. retail feeds the solver backend-referenced time, not raw local raid time; its authored UTC compensates for THAT feed. fix: SkyHourOffset config (default 2.5h) added to the SKY's copy of the raid time only (raid clock untouched, sun drops ~15 deg/h). weather stack round 2: full icebreaker fill machinery ported (refs/curves/gradients/settings ScriptableObjects/external assets by name) + enrich_terminal_weather.py re-derives the never-committed enrichment pass (33 external assets, 4 settings assets; get_nodes_up NOT get_nodes). WeatherDebug backstop + pre-Awake render-asset guard (disables components with unresolved Shader/_close fields — was 7248 null-shader Material NREs/raid). MISSING ASSETS log line = the carrier-pass shopping list (WetShader, tonemapper, DepthWriteShader, circles, CSSpeedTreeWind, Hidden_AmbientHighlihgt). LampController.Awake finalizer swallows the 1164/raid dead-ref stacks (reviver owns the lamps).
- SKY FREEZE ENDGAME + WEATHER SPAM ROUND 2 (2026-08-15b). geometry probe proved the dome rig healthy (scale 1000, sun parented, sunLocalPos == solver's own zenith) — the solver runs ONCE at scene load (on TOD_Time's real-world seed, which is why zenith drifted between raids but froze in-raid) and LateUpdate never fires again. offline replication of method_18 with our params gives sun -11.6 deg at 22:00 — the authored params were right all along. fix: TickSkyTime SELF-DRIVES _sky.method_18() every 10th frame (acoustics-self-init pattern) + re-enables the component if disabled. fps kill #1: SceneLights (staged with weather) ran LampSystem.UpdateComponent over 1164 dead lamp husks per frame = 76k logged exceptions -> SceneLights in SkipClasses + LampController class-wide prefix-skip (Awake/OnEnable/ManualUpdate/TurnLights). fps kill #2: RainController's inner classes call _wetRenderer's lazy material getter REGARDLESS of enabled state -> 49k Material(null-shader) ctors; fixed by (a) merging the 8 build shader names (Hidden/Wet, Hidden/WriteDepth family, Hidden/Tonemapper) into terminal_weather.json external_assets keyed BY FIELD NAME (file#1 pathIDs differ between level600 and level698 — never map by id), (b) guard stuffs Hidden/BlitCopy into any still-null Shader field. 5-BD overcount: topper's single +15s count raced the last walking escort; now counts living blackdiv GLOBALLY and a shortfall must survive a +20s confirm recount before spawning.
- BD ARMORY SIEGE, RETAIL-SHAPED (2026-08-15c). the WIP 1.0 assembly (G:\downloads\Assembly-CSharp.dll, IL2CPP recovery, ilspycmd works on it) ships a FULL dedicated BlackDivision AI: BlackDivisionControlBuildingLayer (cover points inside the target building env), BlackDivisionPatrolDefenceLayer ('BDptrDef', per-bot RandomHorizontal(15m) offset hold around the defended point), plus Patrol/Defence/Assault/Suppress/Flank/Hide/Persuit/boss layers, BotTalkBlackDivision, and new roles (bossBlackDivision, pmcBotBlackDiv, SniperBlackDivision). none of it exists in 4.0. our imitation: TerminalSiegeLogic (Job.Siege) — HangarAmbush squad rushes the armory then holds claimed cover: ~1/3 interior via Covers.FindHidePoint(onlyWithInsideCover:true), rest ring via Covers.FindClosestPoint with an in-bounds+IsFreeById GroupPoint filter; SetOwner/SetFree claims, 45-90s offset re-rolls walk them cover-to-cover, outward LookToPoint on arrival, box-roam fallback where the bake has no points. armory anchor = the Administration_03_small INSTANCE NEAREST TB4 (prefab reused; TB4 does NOT sit on the armory — user correction), patrol box = its renderer bounds +12m/side. BALLISTIC GLASS: panes ship all-zero BallisticCollider; pen gate is PenetrationChance>=eps && power>level && chance-sum>rand (BallisticCollider.cs:99), so sweep sets chance=1/level=0/ricochet=0 on MaterialType.Glass + 'BALLISTIC' name at raid start (BallisticGlassPen config).
- VSRF AI RECON + DEFENSE PORT (2026-08-15d). 1.0 dump vsRF suite: VSRFController (hostility ledger: HIT_TO_BECOME_ENEMY=0, ENEMY_PERIOD=20s, MAX_KILL_TO_FOREVER_ENEMY=5, RegisterHit -> whole-group AddEnemy(vsRFByHits) + spread to boss groups, DISTANCE_TO_CHEAT_VISIBILITY=109), AIPlaceInfoLogicVSRF (territory volumes pump players inside to SetInfoAboutEnemy — controlled-zone tracking), VSRFDefence (prio 100: boss tether MAX_SDIST_FROM_BOSS=900sq=30m, holds group's GetCoverPointMain 'cover in the middle', PERIOD_TO_HEAL=25, PERFECT_DIST=30), VSRFAssault, VSRFTargetLayer (mid-cover hold + ShouldHeal via Medecine.FirstAid.Have2Do/SurgicalKit.HaveWork), separate VSRFSniperLayersStrategy for the roof sniper; PATROL side is vanilla layers (FollowerPatrolLayer/PatrolAssaultLayer) — custom work is all combat-side. 'VSRFDespawn' string has NO class behind it (likely enum member) — despawn claim retracted. WAVE PARITY: all 9 vsRF waves match live field-for-field except the intended role swap (vsRF->ruafRifleman, vsRFSniper->ruafMarksman). PORTED: TerminalRuafDefenseLayer (prio 72, RuafDefense config) — activates only when GoalEnemy exists but NOT visible (the fight's dead air, where retail's positioning shows; visible-enemy combat stays vanilla): claimed cover within 30m of BossToFollow (own pos for the boss), crouch, watch EnemyLastPosition, 25s-cooldown FirstAid.TryApplyToCurrentPart when not under fire. BD posture defaults (2026-08-15): only HangarAmbush = Siege; all other BD Guard-patrol their spawn box; GateAmbushTime default 720 (user has godmode, plays fast — authored 950 assumed live pacing).
- STAGE DIRECTOR DECODED + PORTED (2026-08-15e). the Terminal_AI scene's AIPlaceTerminalWavesController (1.0-only class, absent from 4.0) hand-decoded from level635 path_id 7391: base AIPlaceInfo consumes 104 bytes (4.0 tree says 100 — 1.0 base is +4, and my derived-field decode starts at 104, 0 bytes left). AUTHORING: 3 stages (stage1 = 14 Zone1 zones, stage2 = 18 Zone2 zones, stage3 = Zone2BDPort32/Zone2VSRFExit33/Zone2CivShip34), each completes at RemainPercent=0.10/MimimumCount=1 (kill-driven); UnblocksKeys stage1 -> T0 (the Zone2 wave ladder arms by ATTRITION, walk-in T0 boxes are the parallel path); MigrateData: stage-1 survivors retreat faction-matched (scav->Containers17/Warehouse16, BD->Warehouse18/19, vsRF->PassMiddle15). AIPlaceRoleChecker (52 bytes): below 2 live blackDivision -> raise 'Name777' (placeholder-looking, no observable consumer — not ported). PORT: TerminalStageDirector (StageDirector config) — 5s census of live bots in stage-1 zones, cumulative seen-set floor 8, complete when alive <= max(1, ceil(seen*0.10)) -> AnyEvent('T0') + NoteEvent + seed ByProfile migration recs (Guard+RushTo authored fallback zone; ByProfile lookup precedes the role gate so it works for scav/ruaf too; ByProfile made internal). BlockOnFirstStage zone-blocking NOT ported (bots are job-pinned, BotZone block API unverified).
- PORT BOSS POOL (2026-08-15f, user caught the snapshot bias). Zone2ScavPort29's T4 boss wave rerolls PER RAID server-side — our base.json froze one roll (bossTagilla). the 8 localloot match dumps (1-7.json + client_match_local_start) prove the pool: gluhar x3 (followerGluharSecurity x3, t=5690), sanitar x2 (followerSanitar x3, 5690), tagilla x2 + killa x1 (amount 0, t=5790 — the BossEscortType field carries JUNK on no-escort rolls, e.g. killa's says 'bossTagilla'; Init parses it unconditionally so a substitute must still be a valid role name). wiki adds reshala (3 guards) — unobserved in captures, vanilla bossBully+followerBully used (CORRECTED per user: bossBullyBlackDiv/followerBullyBlackDiv are NOT a terminal reshala variant — they're reshala's 'bully' brain cloned onto Black Division's own bosses, which is also why BossBullyBlackDivLayersStrategy exists). PORT: TerminalPortBossPool (PortBossPool config) — prefix on BossLocationSpawn.ParseMainTypesTypes (BEFORE BossName->BossType parse), equal-odds roll of 5, cached pick re-APPLIED on every parse (idempotent — ParseMainTypesTypes can rerun and the per-raid reset order isn't guaranteed); reset alongside TerminalCrewJobs.Reset in the AIPlaces BotsController hook. RULE REINFORCED: a single match dump is ONE ROLL of anything randomized — check all captures before calling a value authored.
- IMAGE-EFFECT TRAP, BOTH DIRECTIONS (2026-08-15g). Tonemapping/AmbientLight/AmbientHighlight ENABLED = retail's post stack renders over 4.0's own post (hard-edged posterized 'cel-shaded' black). fully SKIPPED = ToDController.Update NREs per frame on its null-unchecked AmbientLightScript.SetSH(SH) (36k exceptions/raid, stutter + WeatherController.LateUpdate dead before driving rain/clouds). resolution: stage-but-FORCE-DISABLE (ForceDisabled set in TerminalWeather) — components exist so refs resolve and SetSH writes fields, enabled=false pre-Awake so they never render. corollary: with ToDController finally alive it drives Atmosphere from sunDir.y per frame via retail curves — TickNightSky now stands down when WeatherController.Instance exists (two-writer guard). general rule earned: when a resurrected system's sub-component misbehaves, check who DEREFERENCES it before cutting it — retail code null-checks some optional refs (AmbientHighlight) and not others (AmbientLight).
- TWO NULL ANCHORS, ONE PATTERN (2026-08-15h). RAIN, finally cornered by the probe: the whole chain was healthy (state machine BUILT, intensity 0.60 flowing, all refs ok) except transform_0 — the rain system's position anchor, assigned at RUNTIME by the camera hookup (Class668.vmethod_8: transform_0 = cameraManager.Camera.transform). the hookup ran during load before the camera existed, NREd on .Camera, our old finalizer swallowed it, nothing retried — rain computed at full intensity around a null anchor for MANY raids. heal: probe re-runs method_0 once the camera exists + direct field set backstop. SIEGE freeze: RushTo was the armory's renderer-bounds CENTER — mid-air at half building height, off navmesh; GoToPoint rejected it silently every 3s and 8 bots stood at spawn logging identical static distances (the 20s telemetry made this diagnosis instant). fix: ArmoryNavAnchor() = NavMesh.SamplePosition of the ground-projected center, TB4 fallback. RULES: (1) any point handed to GoToPoint must be navmesh-sampled first; (2) a swallowed exception in a ONE-SHOT init is a permanent silent failure — always pair swallow-finalizers on init paths with a later retry; (3) movement telemetry (static distance = unpathable target) beats staring at layer logic. also: LockableDoors GameEndedPatch now gated (empty death-popup was LDSession.Instance NRE at raid end — skipping its start hook means its end hook must be skipped too); ambient cutscene silence made STICKY (intro silences before the layer stages; staging now honors the flag); rush yields only to visible enemies within 40m.
- AMBIENT MODEL CORRECTED (2026-08-15i, user: 'tarkov does not have baked lighting and never did'). the long-standing 'brightness lived in lightmaps' story in our comments was UNVERIFIED INFERENCE — retracted everywhere. real lamp mechanism: the runtime lamp SYSTEM (LampController/SceneLights) drives intensities; controllers rip dead -> lamps 0. consequence: TOD-owned ambient IS the retail model. new design: ApplyAmbient derives the fill from _sky.SampleEquatorColor() * AmbientIntensity (static gray only as fallback), written through LevelSettings so the NATIVE applier stays the single RenderSettings writer — preserving the NightVision hemisphere swap and the NvgAmbient slider (the reason TOD's own direct-write module stays muzzled: ONE writer). colors refresh every ~5s. NVG fill = sky fill * NvgAmbient with a slight green cast. ALSO this session: hitch logger ([Perf] window edges + GC delta) for the periodic chop; rain-audio probe (PrecipitationAmbientBlender census vs RainController.IntensityType); rain anchor heal confirm pass. RULE (relearned the hard way): calibrated certainty — a mechanism story repeated across sessions is still a guess until verified; the user's domain knowledge outranks our internal folklore.
- AUTHORED LAMP RESTORE (2026-08-15j, user: 'no way bsg authored every lamp at a single flat intensity'). extract_terminal_lights.py sweeps ALL level files for engine Light components with full world-pos accumulation (recursive local TRS through the parent chain): 7585 lights, intensities 0.0-25.0, colored (red indicators, sodium spots), ~3000 authored-DARK fixtures the flat reviver had been wrongly lighting (map was BRIGHTER than retail). runtime: terminal_lights.json position-keyed on a 1m grid + neighbor-cell search (0.75m match radius — world pos survives the bundle pipeline, GO paths dont); matched lamps get authored intensity*LampAuthoredScale + color + range, authored-zero stays DARK; unmatched fall back to flat LampIntensity. configs LampAuthored (default on) / LampAuthoredScale (1.0 = retail). native CullingLightObject ceilings now follow the per-lamp driven intensity, not the flat value.
- LAMP INTENSITY, THE REAL SOURCE (2026-08-15k, user's catch): culling-managed lamps serialize Light.m_Intensity = 0 because CullingManager drives them at runtime — the AUTHORED value is CullingLightObject._maxLightIntensity, applied per retail's own rule (line 83): driven = _useLightIntensityFromEditor ? _maxLightIntensity : light.intensity. re-extraction: ALL 7585 lights non-zero (top: 3.0 x1237, 0.2 x816, 0.3 x769, 8.0 x645) — the '3000 authored-dark broken fixtures' story is RETRACTED, that was the culling rest state. ambient: literal night-sky sample ~0.01 black was only unplayable BECAUSE the lamps were zeroed; now A/B-able via AmbientSkyLuminance (default true = literal retail sky, lamps carry the scene; false = sky tint at fixed playable luminance). the confound lesson: two lighting bugs landed together and each masked the other's diagnosis — fix one variable at a time before judging the model.


## AUDIO — the full picture (consolidated 2026-08-15)

Terminal's audio is FOUR cooperating systems, all resurrected separately. Sources: level636
(Terminal_Sound — the audio scene), retail SoundBanks carved from the dumps, and our own
authored sound rig. All sidecars in `terminal-client/plugin-data/` (acoustics/, sound_rig*.json).

### 1. Spatial acoustics — SpatialAudioSystem (portals / areas / rooms)
- WHAT: BSG's occlusion/reverb graph. ServerRoomColliderArea rooms, portal volumes between
  them, area volumes — drives indoor/outdoor filtering, muffling through walls, reverb zones.
- SOURCE: level636 component tree -> `acoustics/terminal_spatial_audio.json` (scenes.Terminal_Sound
  section, per-class row arrays with path_id/go/fields; parse_error rows are skipped by staging).
- STAGING (TerminalAcoustics.TryPrepareSpatialAudio / CreateAll): refuses to run until the
  Terminal_Sound scene is LOADED; aborts if comps.Count==0. 607 components rehydrated.
- VOLUME RULE: every portal/area/room collider must be isTrigger=true on the **Triggers layer**
  (MakeAcousticVolume, the ServerRoomColliderArea.Awake convention). A solid-layer acoustic
  volume EATS INTERACTION RAYCASTS — the "fake door" saga. RepairInteractiveLayers() sweeps any
  WorldInteractiveObject that landed on Triggers back to Interactive.
- OCCUPIED-GO GUARD: never attach acoustic components to a GO carrying WorldInteractiveObject.
- INIT: self-driven (TickSpatialInit) with foreign-patch suspend/restore around Initialize
  (icebreaker loot-firewall pattern) so other mods' prefixes can't kill it. The loader also
  calls Initialize prematurely during raid load — Patch_SpatialAudioInitSkip lets the real map
  run it and no-ops it otherwise (TerminalGate reads a location id that isn't set during load;
  the scene-name check TerminalLoaded.Check() closes that gap). NEVER a second blanket prefix:
  the retired legacy patch "staged 607 components then never initialized them" and a later
  variant NREd raid load entirely.

### 2. Ambient layer — AmbientAudioSystem (the environmental bed)
- WHAT: looping/one-shot environmental players, spline emitters, zone calculators, group
  controllers — the map's living background sound.
- BANKS: 44 retail SoundBanks hand-decoded from the dumps (1.0 drift broke typetrees: 28-byte
  header, m_Name, 6 floats CustomLength..Rolloff, 3 u8s, SourceType int, AllowLimitedPlay; clip
  lists recovered by self-validating PPtr carve — every candidate must resolve to a real
  AudioClip). **DUMP-PAIRING RULE: every .assets file loads with the globalgamemanagers.assets
  from ITS OWN dump** — mixing silently resolves every m_Script to the wrong class.
- CLIPS: 368 wavs exported to the SDK (`Terminal_Import/AmbientRetail`), Author 26 scans both
  SoundRig + AmbientRetail dirs, dedupes by name. Missing from bundle: `amb_terminal_outdoor`
  (1 clip — rerun Author 26 + bundle rebuild owed).
- STAGING (TryStageAmbient): 1105 components across the AmbientClasses allowlist (season/event
  variants EXCLUDED — their 1.0 preset structs drift, rows carry parse_error). Discipline:
  deactivate the root ONCE -> create everything (get-or-add; skip GOs the sound rig already
  owns — two players on one GO = doubled audio) -> FillFields + WireRefs -> reactivate ONCE ->
  add AmbientAudioSystem LAST -> Initialize. Banks rebuilt as EFT.SoundBank instances with
  authored scalars, single-variety Environments, HasEnvironment=false.
- CUTSCENE SILENCE IS STICKY: SetAmbientSilenced sets a flag applied at staging time too — the
  intro silences BEFORE the layer stages, so staging must come up muted (the old no-op on a
  missing root let ambient blast over the intro).
- STAGING COST: the 1105-component build lands in ONE frame — the intro cutscene now holds at
  its fade-to-black until AmbientStaged && Weather.Staged (12s cap) so the hitch is invisible.

### 3. Sound rig — the authored orchestration (sound_rig.json)
- Phase-driven players (pre/between/after-cutscene groups) + firefight director with
  shotClips rotation. 74 ambient-sidecar GOs are deliberately "left to the sound rig".
- EVICTION LESSON: a bad clip must be evicted EVERYWHERE — the sparrow survived bank
  far_shoots -> rig players (6) -> firefight.shotClips[0] (the rotation was the repeater).
  Verify the DEPLOYED copy has 0 refs.
- Cutscene AV sync: PlayableDirector.timeUpdateMode = DSPClock (frame hitches cost frames,
  not sync). The rig and the ambient layer both silence during cutscenes.

### 4. Rain / precipitation audio (OPEN as of 2026-08-15)
- CHAIN: RainController.IntensityType (static, live once rain runs) -> PrecipitationAmbientBlender
  (Outdoor + Indoor, rows CLEAN in the spatial sidecar under scenes.Terminal_Sound, staged with
  the ambient classes) -> crossfades dry<->rain loop players. Driver above them:
  EnvironmentSoundBlendSystem.Init collects blenders via GetComponentsInChildren and inits them
  against BetterAudio.Master; its ManualUpdate cascade comes from the ambient system.
- STATUS: rain VISUALS work (transform_0 camera-anchor heal); rain AUDIO still silent. The
  3-pass probe (post-stage / post-heal / post-cutscene) logs blender existence + _precipitationSource /
  _precipitationMixSource / _outputMixerGroup wiring + lastSeen vs controller intensity + blend
  system presence. FIRST PROBE FALSE-NEGATIVE LESSON: FindObjectsOfType skips inactive objects
  and the cutscene deactivates the ambient root — probe rewritten on Resources.FindObjectsOfTypeAll.
- NEXT: read the probe verdict and fix the named link (source wiring vs staging order vs update
  cascade).

### Audio hard rules (each one paid for)
1. Pair every dump file with its own globalgamemanagers.assets.
2. Acoustic volumes: isTrigger + Triggers layer; never on interactive-object GOs; sweep
   interactive objects back off the Triggers layer after staging.
3. Deactivated-create discipline: one root toggle, components before consumers, the system
   component added LAST, Initialize after everything exists.
4. Skip parse_error rows; never stage 1.0-drifted preset classes until hand-verified.
5. A swallowed exception in a one-shot init is a PERMANENT silent failure — pair every
   swallow-finalizer on an init path with a later retry (the rain transform_0 lesson).
6. Probe with Resources.FindObjectsOfTypeAll, not FindObjectsOfType — cutscenes deactivate
   roots and fake "component missing".
7. Cutscene audio ownership: silence flags must be sticky across staging races.
8. Known noise left on the books: ~90 BetterAudio.PlayAtPoint NREs/raid (ambient one-shot edge,
   uninvestigated), SeasonLoopAmbientSoundPlayer 107/122 parse errors (excluded, stretch work).
- RUAF MIXED SQUADS (2026-08-16). full roster verified from RUAFComeHomeServer/db (NOT the wiki names): ruafRifleman, ruafRiflemanSenior, ruafAutorifleman, ruafMachinegunner, ruafMarksman, ruafGrenadier. Supports entries each carry their OWN BossEscortType — mixed compositions are native to the wave format. 8 vsRF waves recomposed in base.json (amounts/difficulties/timers EXACTLY live; only types changed): senior-led squads with machinegunner/autorifleman/grenadier support, big exit fight = senior + MG + 2 riflemen + auto; roof-sniper marksman nest untouched. server-side data — server restart loads it, no client rebuild.
- AMBIENT STENCIL SYSTEM PORTED (2026-08-16, user find + user-rejected workaround). the Terminal_Stencil tree + ambient_portal GOs (Missing Mono Script in SDK) are retail's indoor/outdoor ambient masking: StencilShadow (MeshFilter+Renderer+MB; Awake -> AmbientLight.AddStencilShadow STATIC registry, order-independent) marks volumes in the stencil buffer; AnalyticSource portals (LightIntensity/Multiplicator/FadeNormals/AddAmbient) describe openings; the AmbientLight screen pass consumes both + ToDController SH. extract_terminal_stencils.py: 168 rows (88 stencils, 80 portals) across 15 levels — most stencils ride PROP prefabs, not just the level627 Portals scene. runtime: TerminalStencils.TryStage (name+position matched, 2m tolerance) + AmbientLight GRADUATES from ForceDisabled when AmbientStencil=true (Tonemapping/AmbientHighlight stay forced off — the cel-shade raid was tonemapping-stacked with NO stencils). player-position indoor damping workaround REJECTED by user and reverted — per-surface masking is the point. ALSO 2026-08-16: TerminalLights.cs DESTROYED by a careless `git checkout --` (session uncommitted) and RECOVERED by decompiling our own built DLL (ilspycmd on ManimalTerminalClient.dll -> artifact cleanup: ref-casts, op_Implicit, Object ambiguity, ..ctor calls, stderr nag lines) — comments lost, logic verified by clean build. RULES: commit before sweeping refactors; never `git checkout --` a file with uncommitted work (surgical revert = re-edit); the compiled DLL is a recovery source of last resort.
- THE SHADER.FIND LIE, SOLVED (2026-08-16, 'lets figure it out'). Shader.Find only sees LOADED shader assets — the name-mapped Hidden/* family 'resolved' in the sidecar but returned null in-game because terminal never loads EFT's shaders bundle (vanilla maps do via their asset pipeline). the bundle (EscapeFromTarkov_Data/StreamingAssets/Windows/shaders, 378MB, 555 shaders) contains EVERYTHING: the AmbientLight five, Hidden/Wet, Hidden/Tonemapper, Hidden/Black, Hidden/Snowy. TerminalShaders: GetAllLoadedAssetBundles check -> LoadFromFile -> LoadAllAssets<Shader> -> index by shader.name; ResolveRef's Shader case routes through it. side effect: once indexed, ALL 555 are loaded so plain Shader.Find works game-wide (DepthPhotograper's internal GClass872.Find('Hidden/Black') heals for free). AmbientLight re-graduates behind AmbientStencil, hard-gated on AllShaderFieldsResolved (any null Shader field -> pass stays disabled + named in the log). Tonemapping/AmbientHighlight remain forced off (double-post risk + missing material). WetRenderer now resolves Hidden/Wet -> wetness pass live. RULE: name-resolution in DATA is not availability at RUNTIME — verify Shader.Find against the live game, and when it nulls, find which BUNDLE ships the shader before reaching for a carrier pass.
- AMBIENT PASS EXPERIMENT PARKED (2026-08-16, 4 test raids). full state: shaders loaded+valid (game bundle, TerminalShaders), component fields sane, ToDController curves/gradients sane, SH self-scheduled, stencils+portals restored (63+76, 29 unmatched), AmbientLight+AmbientHighlight pair both running with healed material (Hidden/AmbientHighlihgt built at runtime), one-ambient-owner fill standdown with AmbientPassFloor knob. STILL WRONG: raising fill brightens only cel-highlight patches while blacks stay crushed — diagnosis: the restored stencil volumes mask ambient across the WHOLE screen (wrong transforms/volumes among the 63), portals render as the localized bright patches. next steps are frame-capture territory: RenderDoc the CbStencil/CbAmbient buffers, verify stencil mesh world transforms one by one, compare against a vanilla map's live AmbientLight state via UnityExplorer. config RENAMED AmbientStencil->AmbientStencilExperiment default false (BepInEx persists stale values — the icebreaker kill-switch lesson) so the stable look returns without user action. everything else from the chase SHIPS: game shaders bundle loading (WetRenderer wetness live, DepthPhotograper heals), stencil/portal data staged inert, AmbientPassFloor, one-owner logic — all dormant behind the experiment flag.
- AMBIENT SYSTEM, THE REAL MISSING LINKS (2026-08-16, vanilla-probe diff rounds 1+2). the F8 AmbientProbe on Reserve vs terminal named everything: (1) terminal registered 0 AnalyticSources vs Reserve's 623 — portals are the LIGHT-GIVERS (intensity=2/mult=3/addAmbient=0.3 each; vanilla runs RenderSettings ambient at LITERAL ZERO — the 623 portals ARE the ambient); our portal GOs shipped INACTIVE so OnEnable never registered -> staging now activates the parent chain (stencils 26->59). (2) sources register into hashSet_0 but the DRAW loop iterates list_1 — a cache only built by AmbientLight.RuntimeOptimizePrepare(), which vanilla's raid-start calls and ours didn't; we call it after staging. (3) THE BIG ONE: terminal had NO OnRenderObjectManager — the dispatcher that drives every OnRenderObjectConnector (AmbientLight, AmbientHighlight, WetRenderer, FlareForEffects, LightManager) — so the ambient pass NEVER attached its command buffers (probe: AfterLighting empty vs Reserve's [Custom Ambient, Ambient Highlight]) and the WETNESS pass never drew either; all previous 'pass renders wrong' theories were wrong — IT NEVER RENDERED, the dark raids were our fill standing down for a no-show. manager created at staging (its Start does a FindObjectsOfType catch-up so order doesn't matter). (4) FakeCharacterGI rode in on the camera donor (vanilla chain doesn't run it) — disabled, prime evil-faces suspect. RULE: when a system 'renders wrong', FIRST verify it renders AT ALL (command buffers attached, draw lists non-empty) before debugging its output.
- AMBIENT PASS: FINAL STATE + THE RENDERDOC QUESTION (2026-08-16, 9 test raids). machinery 100% verified live (59 stencils/76 portals registered+cached, buffers attached, culling healed — Culling sub-object fill + [NonSerialized] MaxSqrtDistance recompute via Culling.Update, 3569 NREs -> 0). A/B with AmbientStencilVolumes=false (0 stencils in frame) STILL pitch black -> stencils exonerated. decisive screenshot evidence: lamp BULBS glow (emissive/flare path alive) but cast NO POOLS (deferred lighting accumulation dead) -> the Custom Ambient composite ERASES the lighting buffer on our setup instead of adding (vanilla preserves it). face banding with FakeGI+stencils off = our runtime-built AmbientHighlight material has DEFAULT props (retail's Hidden_AmbientHighlihgt material asset carried tuned values — extractable from sharedassets file#2 ref 2:139 if resumed). REMAINING QUESTION needs RenderDoc: the Custom Ambient draw's blend state + render target on terminal vs vanilla (serialized shader blend reads srcBlend=0/dstBlend=0 — untrustworthy parse). experiment parked behind AmbientStencilExperiment default false; portals-only A/B knob AmbientStencilVolumes. static analysis is EXHAUSTED — do not resume without a frame capture.
- TUSHONKA SDK VALIDATED (2026-08-16, ssh's recommendation — MILESTONE). the module-loader branch of S3RAPH-1M/EscapeFromTushonka-SDK binds ripped scenes to the GAME'S REAL ASSEMBLIES: Unity 2022.3.43f1 (exact game revision), TarkovAssemblyExtractor copies 90 Managed DLLs into Assets/Plugins/Tarkov.Assemblies with dnlib rename Assembly-CSharp* -> Tarkov.Assembly* (we replicated headlessly — scratchpad tushonka_extract tool — because the editor tool can't compile before the assemblies exist: quit the safe-mode dialog, pre-extract, reopen clean). unpack_after_setup.zip ships the gitignored editor tools + STABLE .metas for the DLLs (GUID 865fdfd4ca77fcb4bac580702f09a2e2 for Tarkov.Assembly.dll) matched by the repo's fileid maps (14,963 classes, {className: fileID}). REBIND PROOF: Terminal_Portals.unity — all 55 script refs rebound by script (guid->class from the ASSETRIPPER EXPORT's .cs.metas, 8035 guids — the WTT stubs only cover 786; the stubless backlog classes resolve ONLY via the export) -> AnalyticSource/StencilShadow/LocationScene alive in the inspector WITH RETAIL DATA (intensity 7, door size, culling, linked-stencil slot). CONSEQUENCE: bundles built through this SDK ship components ALIVE — the entire runtime-resurrection class of work (stencils, portals, weather, ambient audio, lamps, TOD) becomes unnecessary for bundle-shipped content, and the parked ambient-darkness mystery likely dissolves (real serialized wiring end to end). migration = full scene-set rebind + dependency move + Author tooling port + bundle rebuild — a major phase, per-bundle all-or-nothing. rebind script pattern proven today.
- STENCILS/PORTALS SHIP MOSTLY DISABLED — AUTHORED STATE (2026-08-17, user hierarchy find). retail terminal levels checked with check_stencil_active.py (m_IsActive + parent-chain walk): StencilShadow 88 rows -> 44 active-in-hierarchy; AnalyticSource 80 rows -> TWO active-in-hierarchy (level627 Portals scene: 27/32 self-active but the PARENT GROUPS are off — exactly the greyed SBG_Terminal_Portals subtrees). nothing in code activates them at runtime: PerfectCulling (CrossSceneContentPortals = empty Koenigz subclass) toggles RENDERERS only (forceRenderingOff/renderer.enabled), never GO active state; DoorAmbuent only repositions its door portal. so the disabled state is what BSG SHIPPED. CONSEQUENCE 1: TerminalStencils.TryStage force-activates every matched parent chain — that resurrects ~4x the authored stencil set and ~40x the portals, including groups BSG deliberately turned off; the parked darkness experiment ran on that off-spec population (extraction STRIPPED m_Enabled and never read m_IsActive). if the experiment ever resumes, filter rows to hierarchy-active only FIRST. CONSEQUENCE 2 (the caveat that cuts the other way): if terminal authored state is ~2 live portals, the 'portals are the ambient' Reserve model (623 live) may simply not describe terminal — BSG may have left terminal's analytic-portal pass mostly unfinished/off, meaning even a perfect restore might not owe us Reserve-like ambient. CONSEQUENCE 3: SDK-built bundles preserve authored active state for free — one more resurrection-era hack (blanket chain activation) that dissolves under Tushonka. RULE: extraction must carry m_IsActive/m_Enabled and restores must honor them — authored-off is data, not damage.
- TUSHONKA MIGRATION EXECUTED (2026-08-17, 'lets do the full migration'). WTT-SDK-2022 Public -> TushonkaSDK, both projects the EXACT same Unity revision 2022.3.43f1 (85497d293fa1), layer tables identical. COPY: Terminal_Import 38,764 files / 11GB (file-count verified) + support set from a full external-guid dependency audit (140 external refs -> TerminalCullingData 26 binary bakes, 14 shared icebreaker clips, 3 shader folders; ~90 were stub-script refs handled by rebind instead). TerminalEval proof scene deleted first (guid collision with the real Terminal_Portals.unity); 'Shader Assets' dedup'd against the SDK's preexisting Shaders folder (same AssetRipper heritage — MuzzleSmoke + folder meta dropped, referenced 5 kept). REBIND: 180,168 m_Script refs across 39 files, 211 distinct classes -> real-assembly dll refs. fileid map COMPUTED from the assemblies (dnlib typedump 8,683+76 top-level types; fileID = int32 LE of MD4('s\0\0\0'+ns+name), pure-python MD4 since openssl3 killed hashlib's) because the repo's shipped map is INCOMPLETE (plain-name keys only, no namespaced classes, 26 entries wrong from flat-name collisions; cross-check agreed on 8,656). guid->class index parsed from stub/export .cs files (namespace decl + stem). NavMeshModifier/Volume/Surface -> ai.navigation package guids; 15 'unknown' guids were Timeline/uGUI/TMP package scripts already valid in-place. KEY CONFIRMATION: the stubless export-guid data survived every Author-pass re-save (35,636 refs incl. all StencilShadow/AnalyticSource) — unity preserves missing-script yaml. Left dangling deliberately: 1.0-only classes (CutscenePlayableDirector, Handler*/Cutscene* set, WireGenerator, AIPlace* — our drivers own that logic), MetaXRAcousticGeometry, PerfectCullingVolume (bake-time-only, absent from game = husk today too; its PC_* binary bakes [PreferBinarySerialization] left husked — but the RETAIL cross-scene culling set (32 CrossSceneGroups + content components) rebound and now ships ALIVE, net upgrade over WTT). BUNDLE BUILD ported (ungated, no game types) with the OUTBOUND assembly swap: build -> AssetsTools.NET unpack to temp FILE (3.7GB LZ4 -> raw exceeds the 2GB byte[] ceiling) -> chunked in-place patch Tarkov.Assembly[-firstpass] -> Assembly-CSharp[-firstpass] (rename is LENGTH-EQUAL by design 15/15+25/25 so raw positional overwrite is safe; firstpass swapped FIRST) -> repack LZ4 -> deploy to the SAME key/path (zero client/server changes). the SDK's inbound AssetBundleLoaderWindow proved the swap direction. 15 Author tools ported GATED behind TERMINAL_AUTHOR_TOOLS scripting define (stub-vs-real API drift = project-wide safe-mode risk; enable + fix per-tool on demand). NEXT: open project (first import of 11GB is LONG), verify compile, spot-check Terminal_Portals/Terminal_Scripts inspectors, Terminal/Build Scene Bundle, test raid. WATCH ON FIRST RAID: runtime resurrection systems can now DOUBLE bundle-native content (acoustics rebuild, weather staging, lamp restore, stencil chain activation) — stand them down one by one as bundle-native equivalents prove out; AmbientStencilExperiment already defaults off. WTT project left untouched as the fallback authoring environment.
- RETAIL REFILL + 1.0 STUB RESTORATION (2026-08-17, 'go through the entire project... restore everything that hasnt carried over'). THE AUDIT (scratchpad tushonka_migrate/audit.py): every real-bound MonoBehaviour in all 39 scenes sampled against a fresh typetree read of the retail levels (1.0 trees from the WIP assembly FIRST via TypeTreeGenerator over managed10 staging dir — 1.0 dll + 4.0 dependency set — then 4.0 fallback, check_read=True validation). scene<->level matching needs DEPTH-2 path signatures (root names alone misassign: Location_Scene/BLOCKERS are everywhere). reliable gap signals = differ/ref_lost/list_lost columns ('missing' is multiline-yaml parser noise): CullingLightObject 565 differ + 170 refs lost, DoorHandle 361 lists lost, BotZone/AIPlaceInfo/TreeInteractivePart refs, TOD/Rain/Ambient chain refs; parse-drifted classes flagged (AreaLight 33, SpatialAudioRoom 14, BotZone 12, AIMinePoint 12) and their partial reads are NEVER emitted (drift-tail lesson). RESTORE = refill_<Scene>.json sidecars (extract_refill.py, ~100 classes) + Author 33 TerminalRetailRefill.cs — fully GENERIC SerializedObject/FindProperty recursion by field name (zero per-class code = zero API-drift compile risk), $ref tokens for same-file object refs resolved by path+2m position, AddComponent when missing, arrays >100k skipped with a warning, external-file refs never emitted (rip wiring preserved). ASSET-TYPED SLOTS (user catch: SoundBank has the document icon — ScriptableObject ASSET in sharedassets, not a component): needs the $asset extension — materialize the 44 retail SoundBanks as project .assets from the existing bank extraction + resolve by name; PLANNED, not yet built. mixer groups / game-resource refs stay runtime-wired (cross-bundle PPtrs impossible — the irreducible runtime core). THE 1.0-ONLY CLASS RESTORATION (user idea, correct one): 98 dangling classes enumerated from the scenes; stubgen (dnlib over the WIP 1.0 assembly + 4.0 assemblies for existence/base checks) emitted 113 stub types — exact namespaces + serialized field layouts, transitive deps + enums, [Serializable] plain classes, 4.0 bases inherited directly (8 BaseEventFilter subclasses got IsFilterPassed=false overrides = inert-preserving; HandlerStateBase subclasses got event+ApplyState stubs; BaseTriggerHandler has no abstract members) — into Assets/TerminalRetail10 with asmdef ManimalTerminal.Retail10 (deterministic md5 guids). AssetRipper yaml 1.0 data lights up by NAME-MATCH the moment the refs bind — no refill needed for these. WireGenerator turned out to live in BSG.Unity.Wires.dll and MetaXRAcousticGeometry/Map in Meta.XR.Audio.dll — BOTH ship among the 90 extracted assemblies -> plain dll rebinds (Meta acoustic geometry comes ALIVE — retail acoustics data for free). rebind: 238 refs -> stubs, 67 -> other dlls; only SinglePlayerApplication + ShadowMapExtractor stay husks (AssetRipper-empty even in the export, 1 ref each). RUNTIME SHIPPING: TerminalBundleBuild deploy now copies Library/ScriptAssemblies/ManimalTerminal.Retail10.dll into the plugin folder — BepInEx loads every plugin dll, bundle MonoScripts bind, so FinallExitZone (the REAL exfil zone, BSG typo — user found it), BotActivityTerminal (cutscene bot-freezer, BotsIsActive + CutSceneChangeBotState subscription), Gate3OpenTrigger/FinalGateOpenHandler, AIPlaceTerminalWavesController etc are readable BY THE CLIENT from the scene at runtime — sidecar replacements become possible per-system. FOLLOW-UPS: $asset SoundBank materialization; user steps = let Unity import TerminalRetail10 + compile, open all scenes, Author 33, save, build bundle.
- REFILL EXTRACTION LANDED (2026-08-17b). TRAP FOUND: TypeTreeGenerator.get_nodes_up needs the FULL type name — bare class names silently fail for every NAMESPACED class and masquerade as drift (the first run 'drift-skipped' the whole audio set, BotZone, Door, all Koenigz classes; the audit's parse_fail column carried the same artifact). fixed via ns lookup from the typedump map. FINAL: 33 refill_<Scene>.json sidecars (~27MB) in Assets/IcebreakerTools/RetailRefill covering ~21k components incl. TreeInteractivePart 7496, CullingLightObject 7438, PatrolPoint 1568, VolumetricLight 1051, SoundPoint 430, all PerfectCullingCrossScene*, StencilShadow/AnalyticSource, full AI wiring set. REAL drift remainder (never emitted): AIMinePoint 705 (runtime AIBake carve owns mines), SeasonLoopAmbientSoundPlayer 122 (known-excluded), SpatialAudioRoom/Portal (runtime acoustics owns), Door 59 (Author 1R live-data rebake owns doors), BotZone 35 (scalars partly carried; wiring restores via PatrolWay/Point), LampController 851-of-1635 (tail drift, 784 extracted). USER STEPS: open Tushonka (TerminalRetail10 compiles + RetailRefill imports), open ALL terminal scenes additively, Terminal/Author 33 - Retail Refill, save all, Terminal/Build Scene Bundle (deploys bundle + Retail10 dll), test raid.
- FIRST TUSHONKA BUNDLE SHIPPED (2026-08-17c). tags: game TagManager (20 tags incl OpticCamera/SpawnPoint/CoverPoint/patrolPoint) merged into Tushonka ProjectSettings — edit-mode CompareTag spam gone; remaining edit-mode NRE spam (TodLightSetter/RippleController/null-shader) is live-component-in-editor noise, harmless. stub asmdef needed Unity.Timeline reference (safe-mode round). AUTHOR 33 RESULTS: ~11.6k components filled across 35 scenes, 0 unmatched / 0 added (path+pos matcher perfect), ~370k refs rewired with ~60 failures total (54 in Design_Stuff — 1.0-husk targets); drift ledger tiny+consistent: LocationScene:6/scene, AIPlaceInfo:17, AIPlaceLogicEventRaise:16, LampController+CullingLightObject stragglers. BUILD: 3747MB, assembly swap 208 occurrences (== distinct rebound classes; Retail10 MonoScripts untouched by design), deployed to SPTDev + SPTDevFika incl ManimalTerminal.Retail10.dll. NEXT: first raid on a bundle-native map — watch for runtime-resurrection DOUBLING (ambient audio, weather staging, lamp restore, stencil chain activation); then stand runtime systems down one at a time.
- AMBIENT MYSTERY: COMPOSITE CONFIRMED AS THE LIGHT-EATER (2026-08-17d, one-toggle repro). the tushonka bundle-native raid ran the retail ambient pass FULLY FED for the first time (probe: 39 self-registered authored stencils + 76 portals w/ draw cache, all 5 shaders resolved, AmbientHighlight up, buffers attached like Reserve: StencilShadow@BeforeLighting + [Ambient Highlight, Custom Ambient]@AfterLighting) — screen still dark-crushed; user disabled the AmbientLight COMPONENT live via UnityExplorer and everything went normal instantly. VERDICT: the Custom Ambient composite subtracts/erases light even on retail-faithful inputs — every garbage-input theory (wrong volumes, no registrations, null shaders, missing dispatcher) is now ELIMINATED. remaining suspects, sharply scoped: (1) the SH the pass composes with — vanilla's ToD path computes sky SH into the pass; our stand-in may deliver black (night atmoBrightness 0.213); (2) runtime-built material props — retail's Hidden_AmbientHighlihgt material asset carries tuned values ours defaults (sharedassets file#2 ref 2:139, extractable). NEXT (no RenderDoc needed): extend the F8 probe to dump the pass's SH shader globals + material_3/AmbientMaterial properties+keywords, capture on terminal AND Reserve, diff. NOTE: AmbientStencilExperiment was ON in the persisted config (user-flipped during the old experiments) — that is what staged/graduated the pass this raid; =false remains the stable playable look. ALSO first successful tushonka-bundle raid this session after three masked aborts: (1) SmartGrip.Reset infinite NRE loop in door-interaction exit (refill wired DoorHandle->SmartGrip refs whose own Pivot is still rip-null — swallow finalizer; proper fix = add SmartGrip/GripPose/LimbIK to the refill); (2) spatial-audio natives alive-but-broken -> scrubbed 7 classes; (3) SpatialAudioCrossSceneGroup's AWAKE-registered static AllCrossGroups registry handed BSG a list of scrub-DESTROYED rooms (the registry lesson AGAIN — its OnDestroy clears the registry so scrubbing the GROUP is the whole fix). diagnostic hooks now PERMANENT: TarkovApplication.method_39 prefix names every 'error during matching' exception; PreloaderUI.ShowErrorScreen/ErrorScreen.Show prefixes name every swallowed dialog. TRAP RECORDED in repo CLAUDE.md: new [HarmonyPatch] classes MUST be added to Plugin.cs's explicit Patch() list — unlisted classes compile and silently never apply (cost two test cycles). ALSO: gate defensive patches by SCENE NAME not TerminalGate.On when they can fire during matching (transit-gate-blindness); Author 33 must be run with ALL scenes open — Terminal_Light was missed on the first pass and shipped the light scene unfilled (the 'extra dark' layer, since fixed).
- AMBIENT MYSTERY RESOLVED — NOT A BUG, A MODEL COLLISION (2026-08-17e, SH/material probe diff + user's Shoreline control). the extended F8 probe dumped the composite's inputs on Shoreline (working) vs terminal (dark): SH globals IDENTICAL to the 3rd decimal (terminal marginally brighter), material_3/WriteScreenAmbient props identical, highlight material identical, no keyword/queue deltas — the pass is NOT starved and NOT built wrong. the user's control test named the mechanism: on Shoreline NIGHT, disabling AmbientLight went PITCH BLACK ('all the lights shut off') — on vanilla maps the Custom Ambient pass IS the visible night ambience (RenderSettings ambient authored to literal zero; portals+SH carry everything). VERDICT: the pass REPLACES frame ambient rather than adding — on terminal it faithfully overwrites our flat playable fill (0.8) with retail's near-black night SH = 'dark blackness over everything'. this also explains the parked-era 'raising fill brightens only patches' (fill survived only outside pass coverage) and closes the 9-raid hunt: every component of the pass was working the whole time once fed; the collision was OUR FILL vs RETAIL'S REPLACE. terminal reads darker than vanilla night because BSG authored only ~2/80 portals active on this map (2026-08-17 authored-state finding — the map's night was never portal-carried like Reserve's 623). LOOK CHOICE, not bug: (A) flat fill, experiment off = stable port default; (B) retail pass = authentic near-black night — pending one check: do refilled bundle-native lamp POOLS survive with the pass on (parked-era 'bulbs but no pools' may have been the lamp standdown, not the pass); if yes, (C) hybrid = pass on + raised portal intensities. RULE learned: when a restored system 'breaks' the look, check whether it REPLACES something a workaround was providing — two rendering models can both work alone and fight stacked.
- AMBIENT PROMOTED: 'OUR AMBIENT, STENCIL-DARKENED' (2026-08-17f, user call). the resolution shipped as a feature: prefix on AmbientLight.SetSH + AmbientHighlight.SetSH tunes the harmonics BEFORE the pass consumes them — AmbientSHBoost (multiplier on the sky term, default 1) + AmbientSHFloor (adds fraction of RenderSettings.ambientLight i.e. our flat-fill color to the SH constant term, default 1.0 = fill-brightness base). result: base ambient is ours, StencilShadow volumes darken their pixels per-area (geometric, through windows/doors — NOT player-position triggers), analytic portals glow on top, lamp pools untouched. AmbientStencilExperiment RETIRED — key renamed to AmbientRetailPass default TRUE (stale config values die with the rename); off = flat-fill-only fallback. both SH knobs live-tunable in the F12 config manager. shared Tune() helper in TerminalRaidFixes (AmbientSHTune), patch classes REGISTERED in Plugin's explicit list.
- AMBIENT FINAL SHAPE: OLD SYSTEM + WORKING STENCILS (2026-08-17g, user call after the posterize raid — hard black/white banding on faces AND walls with the full pass on). the artifact was AmbientHighlight: runtime-built material with DEFAULT props where retail ships a tuned asset (sharedassets file#2 ref 2:139). RESOLUTION: AmbientHighlight force-disabled BOTH ways (graduation branch removed in TerminalWeather + bundle-native copy added to the scene scrub) — re-enable only WITH extracted retail material props (queued). shipped configuration: AmbientLight pass ON (it IS the stencil consumer — no pass, no stencil darkening), AmbientSHFloor carries the old flat-fill brightness as the base, stencil volumes darken per-pixel, portals glow, characters get plain lighting. RULE: the retail ambient system is three separable passes — stencil marks, screen composite, character highlight — and they fail independently; graduate them independently.
- STENCIL-LITE: HOMEMADE DARKENING, RETAIL PASS RETIRED (2026-08-17h, user call: 'get rid of it. make your own stencil shader if you have to. outdoors brighter, indoors not bright as fuck'). NEW TerminalStencilLite: flat fill stays THE ambient; each authored StencilShadow volume MESH (bundle-native, activeInHierarchy = authored actives only) is drawn into the deferred EMISSION gbuffer (GBuffer3 — where flat ambient lives) with multiply blend at CameraEvent.BeforeLighting: Hidden/Internal-Colored (always-shipped builtin, runtime-configurable _SrcBlend=Zero/_DstBlend=SrcColor/_Cull=Front/_ZTest=GEqual — the backface volume technique: only pixels INSIDE the volume darken, per-pixel through windows/doors), per-volume factor 1-Ambient.a*strength via MPB. pre-lighting = indoor lamp pools UNTOUCHED. configs: StencilDarken (default on), StencilDarkenStrength (0-1.5, scales authored alphas). AmbientRetailPass default -> FALSE (full pass + SH knobs remain as opt-in curiosity). ticked from Plugin.Update (attach-once per camera, detach on gate off). POSTFX: the FPS camera already carries PostProcessLayer + global PostProcessVolume (Cam2_z4_Profile_Woods, camera-donor cargo) — in-game PostFX menu expected to work, user to verify.
- RETAIL AMBIENT MACHINERY DELETED (2026-08-17i, user call: 'dead code useless bloat'). removed wholesale: TerminalStencils.cs (the entire runtime stencil/portal restore — matching/filling/chain-activation/RuntimeOptimizePrepare/OnRenderObjectManager creation; bundle natives self-register and only TerminalStencilLite consumes them now), terminal_stencils.json sidecar (repo + deployed), the SH tune patches (AmbientSHTune + both SetSH prefixes), the TerminalWeather graduation gates + AllShaderFieldsResolved/HealHighlightMaterial helpers (ForceDisabled = permanent for Tonemapping/AmbientLight/AmbientHighlight), the TerminalLights one-owner standdown block, and FIVE configs (AmbientRetailPass, AmbientPassFloor, AmbientSHBoost, AmbientSHFloor, AmbientStencilVolumes). SURVIVING ambient surface: AmbientIntensity + AmbientSkyLuminance + NvgAmbient (the fill), StencilDarken + StencilDarkenStrength (stencil-lite), CharacterFakeGI. the scrub still kills native AmbientLight/AmbientHighlight. NOTE: stale retired keys linger in users' cfg files harmlessly (BepInEx ignores unbound keys).
- STENCIL-LITE v2: DEPTH-FAIL MARKING (2026-08-17j, user: 'i can see all the stencils through the walls'). the Internal-Colored one-pass trick was geometrically wrong — a lone backface GEqual test also passes for every surface IN FRONT of the volume, projecting silhouettes through walls. proper fix: Manimal/StencilDarken shader (SDK Assets/IcebreakerTools/Shaders, deterministic meta guid, ships via NEW Terminal/Build FX Bundle menu -> plugins/ManimalTerminal/terminal_fx.bundle, seconds not a scene rebuild; client loads it directly — NOT under streamingassets so the bundle host ignores it): 3 passes — backfaces ZFail IncrWrap, frontfaces ZFail DecrWrap (two-sided depth-fail: net nonzero stencil = pixel inside the closed hull, camera-inside-safe, arbitrary meshes), then Blend Zero SrcColor multiply on GBuffer3 where Comp NotEqual 0 with Pass Zero clearing marks per-volume. stencil stomping at BeforeLighting is retail-proven (their CbStencil does the same). USER STEP: open Tushonka -> Terminal/Build FX Bundle once, then raid.
- POST-REFILL REGRESSION SWEEP (2026-08-17k/l). refill fallout + fixes: (1) LOOT/GEARTAX: refill hole-punched LocationScene registries (54 failed $refs in Design_Stuff) -> runtime HealLocationScenes rebuilds ALL object arrays from live scene scans at sceneLoaded (282 containers in Design_Stuff) — AND must refresh dictionary_0 (Awake CACHED the holed arrays; every consumer incl the loot binder reads the DICT, not the fields — the icebreaker awake-copies lesson bit a second time: 'no bound valberg safe' survived the array-only fix). (2) BOTS: marker-mask heal measured only 5/109 markers lacking Bot category — mask drift NOT the cause; user called it: QUESTING BOTS spawns PMC groups itself ('No valid spawn points' x22, one floating invisible PMC, everything else starved). TerminalQuestingBotsOff (LockableDoors pattern): prefix-skip PMCGenerator/PScavGenerator DECLARED overrides CanSpawnBots(false) + GetNumberOfBotsAllowedToSpawn(0) behind TerminalGate — generation still completes (muting GetMaxGeneratedBots would hang QB's delay_game_start wait; GeneratorProgress divides by it). QB fully active on other maps. (3) STENCIL-LITE cutscene overlay: detach during BOTH cutscenes (intro FinishedAt<0 while Available; TerminalAttackCutscene.PlayingNow). (4) AmbientColor alpha-as-enable footgun (user set FFFFFF00 = disabled): replaced with explicit AmbientColorEnabled bool.
- AMBIENT SURFACE, FINAL SIMPLIFICATION (2026-08-17m, user calls). AmbientColorEnabled bool removed (redundant — intensity 0 IS off) and AmbientSkyLuminance removed entirely (retail-system semantics with the retail system gone; the sky-sampling branch in ApplyAmbient deleted with it). the WHOLE ambient model is now: RenderSettings fill = AmbientColor tint x (0.16 x AmbientIntensity), carved per-pixel indoors by StencilDarken/StencilDarkenStrength. four knobs, one code path.
- THE BOT KILLER + DARK LAMPS, BOTH NAMED (2026-08-17n). GEARTAX CONFIRMED WORKING (dictionary_0 refresh — the awake-copies lesson, second billing). BOTS: QB's ActivateBossesByWavePatch.shouldSuppressBossWave ate ALL 31 released waves ('too many bosses' — terminal's whole population is BossLocationSpawn waves); gated via prefix SkipFalse on terminal (TerminalQuestingBotsOff now mutes generators + suppression). LAMPS DARK + SLIDERS DEAD: ApplyLamps had `if (!CullingManager.Instance) DriveLamp(...)` — pre-tushonka the manager was a husk so the driver always ran; the live bundle manager made the gate skip ALL 7438 culling-wrapped lamps (raw authored values, no scale). fix: drive ALWAYS, then sync _maxLightIntensity/float_1 to the driven value so the live culling system re-applies OURS. BUSHES FLOATING (billboard offset until close): impostor system (AmplifyImpostors.AmplifyImpostor + EFT.Impostors array classes) scrubbed — meshes carry the look. RULE: every `if (native system dead) do-our-thing` gate written in the husk era INVERTS its meaning the day the native system comes alive — audit all such gates after a migration.
- THE ZERO-BOT ROOT CAUSE, NAMED AND FIXED (2026-08-17o, three witness raids). the chain that finally converged: server generated the FULL wave set (350 bots incl 100 blackDiv + all ruaf roles) so generation was never it; witnesses showed waves ARRIVING at BotsController.ActivateBotsByWave with correct zones/TB-triggers but NEVER reaching BossSpawnerClass placement; the gap = `if (BotSpawner_1 != null && this.CanSpawn)` — and BotsController.Init sets **CanSpawn=false when every BotZone's serialized SpawnPointMarkers list is empty** (zones with empty lists are DROPPED; all-dropped = spawning off, silently, for the whole raid). terminal's zones ALL ship empty (rip-lost wiring — THE USER'S FIRST INSPECTOR SCREENSHOT SHOWED IT and the runtime-binding assumption dismissed it, wrongly). FIX: BotsController.Init prefix rebuilds zone->marker wiring (parent-chain zone first, nearest-zone fallback, sets marker.BotZone back-ref) before Init reads the lists. ALSO LEARNED en route: boss waves carry ignoreMax so CheckOnMax is skipped (cap was never it); QB start-delay forced off on terminal (double wave-holder); most TB-triggered waves are AUTHORED for t=620-1111s so bots arrive LATE by design — short test raids undercount even when healthy. witness patches (activation/placement/defer + cap) kept permanently. bushes: impostor scrub (14k comps) did NOT fix floating bushes; cross-scene merged tree batches (PerfectCullingCrossSceneContentTrees + CrossSceneCullingTreePreProcess) scrubbed as next theory — if still floating, per-bush UnityExplorer inspection next, no more shotgunning.
- SLOW SPAWNS NAMED (2026-08-17p, witness data finally complete once the grep pattern was fixed): the chain WORKS — 5 'placement STARTED' = the census bots — but 14 placements FAILED at BossSpawnerClass.method_2's FIRST line, `if (!BotSpawner.IsProfilesLoaded) return false`, then crawled BSG's retry queue. our SpawnGate released waves into an unloaded spawner. FIX: Open now also requires BotSpawner.IsProfilesLoaded after the cutscene (480s failsafe releases unconditionally, capping deadlock risk). ALSO stepwise activator now logs SUCCESS per bot ('activated clean state=...') — silence around a standing bot = activation never invoked, one line = activated-but-brain-idle, failed steps enumerate themselves. NOTE for pacing expectations: wave times are AUTHORED long — TB1 waves at t=5820s (97 min!), most TB waves 620-1111s — terminal is a slow-burn map by retail design; judge spawn health by the t=40/-1 VSRF squad and the trickle, not totals in a 5-min raid.
- TUSHONKA EXPERIMENT SHELVED, FULL ROLLBACK (2026-08-17q, user call: 'revert to the last commit before the sdk editor slop'). client working tree restored to b7e37e1 via `git stash push --include-untracked` — the ENTIRE tushonka-era client (scrub additions, StencilLite, QB gates, spawn heals, witnesses, ambient simplification) lives in stash 'tushonka/sdk experiment client work 2026-08-17 (shelved)'; recover with git stash pop / git stash branch. docs kept (this playbook + repo CLAUDE.md restored from the stash). old client rebuilt + deployed, terminal_stencils.json sidecar redeployed. CONFIG TRAP DEFUSED: b7e37e1 predates the experiment parking and binds AmbientStencil DEFAULT TRUE — and the cfg still carried AmbientStencil=true from that era; pinned false (the rename-kills-stale-values lesson cuts BOTH ways on rollback: reverting code resurrects the OLD key). USER STEP: rebuild + deploy the old bundle from WTT-SDK-2022 Public (Terminal/Build Scene Bundle); optionally delete ManimalTerminal.Retail10.dll + terminal_fx.bundle from the plugin folder. POST-MORTEM aids preserved in this playbook: the bundle-native map DID load and play — the residual issue set (bot activation stall, spawn pacing, bushes) traced to husk-era-gate inversions, most identified; if ever resumed, start from the 2026-08-17 entries, the stash, and the reverted client's own comment that ObservedCullingManager ABSENCE causes floating-gear ghosts (the tushonka symptom was its PRESENCE-with-broken-data — the gate inversion rule, both directions).
- STENCIL-LITE CHERRY-PICKED INTO THE OLD-BUNDLE WORLD (2026-08-17r, user: 'you brought back the stencils right'). the one kept piece of the shelved experiment: TerminalStencilLite rewritten SIDECAR-DRIVEN — the authored volume MESHES survive on the husk GOs (MeshFilter is native), so rows from terminal_stencils.json name+position-match the GOs (the TerminalStencils matcher pattern, 2m) and supply each volume's authored Ambient.a; same ManimalStencilDarken 3-pass depth-fail shader from terminal_fx.bundle (loaded straight from the plugin folder). configs StencilDarken/StencilDarkenStrength re-added to the reverted Plugin + a new Update tick (the old Plugin had none). cutscene detach gates kept (IntroCutscene.FinishedAt / AttackCutscene.PlayingNow — both classes predate the experiment and survived the revert). works on ANY bundle since it needs no live StencilShadow components — this is the durable form of the feature.
- QUESTINGBOTS PER-MAP MUTE, BOTH MAPS (2026-08-17s). restored TerminalQuestingBotsOff from the stash (QB's own auto-disable is startup-global keyed to Unda/Acid's-BPS detection — no per-map switch exists upstream) and cloned as IcebreakerQuestingBotsOff (IceGate.On gate, wired after icebreaker's PatchAll). six gates each: PMCGenerator+PScavGenerator CanSpawnBots(false)/GetNumberOfBotsAllowedToSpawn(0) [DECLARED overrides — virtual-base lesson], ActivateBossesByWavePatch.shouldSuppressBossWave(false) [the wave-eater], GameStartPatch.IsDelayingGameStart getter(false) [double-holder]. GetMaxGeneratedBots deliberately NOT muted (QB's start-wait divides by it / would hang). QB untouched off-map. stencil-lite fx bundle needs NO rebuild for the rollback — terminal_fx.bundle already deployed and the shader is unchanged; only the SCENE bundle rebuild from WTT is the rollback step.
- QB VINDICATED AS THE SPAWN SABOTEUR + POP-IN FIX (2026-08-17t, user called it, twice). with QuestingBots fully REMOVED, RUAF spawned at raid start on the old bundle — QB interfered through channels the 6 gates missed (it also patches the game's TryToSpawnInZoneAndDelay with its own retry pacing, plus BotsControllerInitPatch cap adjustments). LESSON: muting a takeover mod method-by-method is whack-a-mole — QB has no per-map off switch and the reliable per-map answer is not installing it / a load-order-level disable. the 6-gate mute stays in both clients as harm reduction if QB returns. POP-IN: SpawnGate now releases when the attack cutscene STARTS (PlayingNow) instead of ends — the ~48s show covers the staggered placement so squads are pre-placed when control returns; profiles-wait (IsProfilesLoaded) kept from the stash cherry-pick.
- GATES_EXPLOSION DECODED (2026-08-17u, user find in Terminal_Design_Main). the retail gate = BSG's generic TRIGGER-NETWORK system, fully data-authored: TriggersPrefab suffixes every id (_3037225082), handlers chain via string triggers on GClass3592 (4.0's TriggersEmitter — ALIVE in SPT). three paths: solo switch hold (RequirementsWithFailedTrigger -> PlantAction hold -> gates_opened_solo -> HandlerAnimator IsRemoved), TWO-PLAYER co-op (HandlerContinuousGroupRequirement, 2 players, 30s timeout, dual PlantAction holds -> gates_opened_duo_confirmed), and the BOMB (Switch_bomb -> requirements(SZ-1) -> PlantAction 5s -> place_bomb: HandlerRemoveItem consumes itemId + charge prop shown + switches retired -> HandlerDelay 10.0s -> bomb_explosion: HandlerExplosion with REAL mine-settings damage + sound + quest condition + prop hidden). recon script: scratchpad tushonka_migrate/gates_explosion_recon.py (full component dump of level629 subtree incl per-field values + cross-refs). 4.0 AVAILABILITY: TriggersPrefab/HandlerAnimator/HandlerDelay/HandlerExplosion/HandlerInteractable/HandlerGameObjectState/HandlerGOState/GameObjectStateSync/AnimatorSync all EXIST AND PARSE in 4.0; Switch exists but 1.0 layout drifted. MISSING (1.0-only, fields+behavior fully recovered from the WIP assembly via ilspycmd — recovery stripped [SerializeField] so the stubgen emitted them EMPTY, but code bodies name every field): HandlerPlantAction (_triggerId/_onFailed/_onSuccess/_plantingTime=5/panel+failed text, client-authority), HandlerRemoveItem (itemId consume), HandlerRequirementsWithFailedTrigger (input/output/failedOutput + ODIN-serialized requirements — payload needs extra decode), HandlerContinuousGroupRequirement (register/unregister/ready/reset/timeout, count=2, 30s), HandlerPlayerNotification/HandlerPlaySoundAdvanced/HandlerCompleteQuestCondition (simple). BUILD PATH: implement the six as real client MonoBehaviours against GClass3592 + resurrect the authored graph from the recon sidecar (icebreaker resurrection pattern, but the ENGINE is native) — retail choreography incl co-op, and a fuse that actually deals damage; icebreaker's chain door hand-rolled PlantAction without knowing BSG had one.
